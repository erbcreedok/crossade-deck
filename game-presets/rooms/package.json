{
  "name": "@game-presets/rooms",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A game-kit preset add-on: the bridge between the hub and a game — the list of tables, the search and the making of a new one. It knows nothing of our server and nothing of any game: it is given rooms and hands back three answers.",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "module": "./src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@crossade/look": "*"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
