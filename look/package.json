{
  "name": "@crossade/look",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "What Crossade Deck looks like: the palette, the surfaces built out of it, and the text roles. Worn by the hub's shelf and by every desk alike.",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "module": "./src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "game-kit": "*"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
