# Правила каталога и визуальной модели

Всё, что ниже, — требования владельца, высказанные в работе. Каждое появилось после того, как было
нарушено. Список нужен, чтобы не выяснять их заново: правило без записи и без теста живёт до первой
пересборки контекста.

Колонка «сторож» — чем правило держится сейчас. `—` значит «ничем, держится только вниманием».

## Каталог: структура

| правило | сторож |
|---|---|
| Одна стори на раздел. Страниц-перечней («все варианты в ряд») нет: варианты различает АРГУМЕНТ, а сравнивать их — работа боковой навигации | — |
| Отдельная стори заводится, только когда меняется СЦЕНАРИЙ или способ сборки, а не значение свойства | — |
| Примитивы стола (карта, стопка, фишка, кнопка, цензура) — в `UI-kit`. Механики (зоны, порт команд, доска) — в `Mechanics`. Анимации — в `Animations` | `catalogStructure.test.ts` |
| Галерея всех вариантов уместна на вкладке **Docs** (её читают, сравнение нужно) и неуместна в режиме стори (там отлаживают, соседи мешают) | — |
| Рычаг виден только когда он ДЕЙСТВУЕТ. Крутилка, которая сейчас ни на что не влияет, — обман (`if: { arg, eq }`) | — |
| В панели — СВОЙСТВА. Действия («перевернуть», «сжечь») — кнопками на сцене | — |
| Служебных объектов в панели нет: пресет по имени, внутренние структуры — не свойства компонента | `catalogStructure.test.ts` |

## Каталог: панель и код

| правило | сторож |
|---|---|
| `name` — настоящее имя свойства, английское. Русский только в `description` | `argNames.test.ts` (весь каталог: имена вычитываются из исходников стори) |
| Колонка **Default** заполнена везде (`argTypesEnhancer` из начальных аргументов) | `argTable.test.ts` |
| Тип печатается, только если он ЧТО-ТО СООБЩАЕТ: союз у списка выбора. `boolean` у тумблера и `number` у слайдера — шум, `string` у списка из четырёх вариантов — ложь | `argTable.test.ts` |
| «Show code» печатает код КОМПОНЕНТА с аргументами этой стори, а не исходник стори и не пересказ JSX | — |
| Описание рычага объясняет, что он значит, а не повторяет его имя | `argNames.test.ts` |

## Модель: высота (`z`)

| правило | сторож |
|---|---|
| Две оси и обе названы: ЭКРАН (x, y — место на столе, Y растёт вниз) и ВЫСОТА (`z` — к зрителю, 0 у лежащего) | `plane.test.ts` |
| `z` — источник; экранный подъём и тень — следствия. Ничто не двигает предмет по экрану «чтобы выглядел выше» | `plane.test.ts` |
| Высоту даёт только то, что МЕНЯЕТ РАЗМЕР предмета. Idle-дыхание размера не меняет → это экранная декорация, а не высота | `plane.test.ts` |
| `z` есть у каждого элемента как свойство. У всего, что лежит на столе, — 0 | — |
| Подъём в полёте (`move.lift`) — высота, а не сдвиг позиции: иначе тень летит по той же дуге и перспектива пропадает | `moveStyles.test.ts` |

## Модель: поза и дыхание

| правило | сторож |
|---|---|
| Ось зовётся `pose`, а не `rest`: `rest` — одно из её ЗНАЧЕНИЙ, и `rest: "rest"` не читается | — |
| Значения позы — `rest` / `lifted` / `held`. Не `floating`: это читается как имя анимации, а поза — состояние | — |
| Дыхание (`idle`) — СВОЯ ось, доступная в любой позе. Поза задаёт лишь умолчание: поднятый дышит, лежащий нет | `plane.test.ts` |
| Явное `idle` перебивает умолчание позы в обе стороны: лежащая карта дышит, поднятая стоит | `plane.test.ts` |
| Дышит не только карта: фишка и фигура — тем же законом, тем же пресетом | — |
| Условие сна цикла идёт по САМОМУ дыханию, а не по позе: неподвижная поднятая карта отпускает цикл | — |

## Модель: тень

| правило | сторож |
|---|---|
| Один закон на все предметы. Форма (прямоугольник карты, эллипс фишки) — параметр, а не второй файл | — |
| Лежащий предмет отбрасывает тень СВОЕГО размера, ни пикселем больше | `plane.test.ts` |
| Тень едет в ТУ ЖЕ сторону, что предмет. Высоту показывает ЗАЗОР, а не встречное движение | `plane.test.ts` |
| `attach` — режим, а не число: `element` (приклеена, размер постоянный) или `table` (лежит, растёт и отдаляется). Промежуточного не бывает | `plane.test.ts` |
| Тень ВЫВОДИТСЯ из состояния предмета каждый кадр. Отдельных «теневых анимаций» под каждый эффект нет | — |
| Маска эффекта (горение, шреддер) режет и тень тоже | — |
| Тени одного слоя сливаются в ОДНУ маску: иначе в пересечениях темнеют. Между слоями — не сливаются | — |
| У кнопки тень своя, вне общего пасса: в общей маске она затемняла бы карты под собой | — |

## Компоненты

| правило | сторож |
|---|---|
| `disabled` гасит ВИД, а не только клик | — |
| Габарит и подгонка текста — общая арифметика (`ui/boxFit.ts`), а не своя у каждой коробки | `boxFit.test.ts` |
| Границы `min/max` применяются последними — в том числе к явным `width/height` | `boxFit.test.ts` |
| Размер коробки (`fit`) и размер текста (`textSize` / `textShrink` / `textGrow` / `textFit`) — независимые оси | `boxFit.test.ts` |
| Раскладка стопки — ФУНКЦИЯ. Направление, шаг, доворот — параметры одной линейной раскладки, а не список имён | `stackLayout.test.ts` |
| Стиль анимации передаётся ОБЪЕКТОМ без регистрации. Реестр — список готовых и точка для сериализуемых конфигов | `styleRegistry.test.ts`, `moveStyles.test.ts`, `stackLayout.test.ts` |
| Внутренние числа стиля (`knobs`) — данные: панель строится по ним автоматически | `styleRegistry.test.ts` |

## Движок

| правило | сторож |
|---|---|
| Глубина возвращается ПО ПРИЛЁТУ: отпущенная карта не ныряет под соседей всю дорогу домой | — |
| Догоревший элемент сносится вместе с Pixi-узлом, иначе последний кадр эффекта застывает на столе | — |
| «Стоп»-качание — ответ на ПОПЫТКУ ТАЩИТЬ (порог 6 px), один раз за жест, а не на тык | `inputRouter.test.ts` |
| Появление — событие доски: не переигрывается на пересборке и восстанавливает уничтоженный элемент из спеки | — |
| Колесо не перехватывается, если панорамировать некуда: страница обязана скроллиться | — |
| Канвас занимает ВИТРИНУ, а не фрейм: не больше кадра (иначе край срезан и достать его нечем) и не крупнее 1:1 (иначе кнопка раздувается просто потому, что место есть) | `kitExtent.test.ts` |
| Хост меряет ФРЕЙМ, а не свою коробку: мерить то, что сам же и сжимаешь, — кормить расчёт его результатом | — |
| Идентификаторы английские; русский — в подписях UI и описаниях | `argNames.test.ts` |
| «Переместить» значит «теперь предмет ЖИВЁТ здесь»: команда `move` переставляет дом, иначе возврат из драга отыгрывает перемещение назад | — |
| Дроп разрешается по координате ПАЛЬЦА. Тело едет пружиной и отстаёт: считать по нему — спорить с зоной, которая уже выбрана пальцем | — |

## Не работает

Известных поломок нет. Два прежних места закрыты 2026-08-02 (см. `git log`):

- дроп в слот на доске — причина оказалась вне `BoardZone`: команда `move` не переставляла ДОМ, и
  возврат из драга отыгрывал перемещение назад; плюс слот выбирался телом фигуры, отстающим от
  пальца. Проверено руками: пустая клетка, занятая (swap), запрет по правилу;
- панель «Код» — подписывалась на канал позже, чем превью присылало код. Берёт `channel.last()`.

**Важно про проверку:** `.storybook/manager.tsx` подхватывается только при СТАРТЕ Storybook.
Правку панели на уже запущенном сервере не увидеть — поднимайте инстанс заново.
