# SOLITAIRE-REBUILD — чекпоинт и что делать дальше

Документ для агента, который подхватывает работу. Читать целиком до первой правки.
Общий трек пасьянса — [`SOLITAIRE-HANDOFF.md`](./SOLITAIRE-HANDOFF.md), он описывает E1/E2; этот
файл про то, что случилось с E3–E8 и почему их надо переделать.

## 0. Одной строкой

**ПЕРЕПИСЬ ЗАКРЫТА (2026-07-31, ветка `feat/solitaire-scene-engine`).** Пасьянс сидит на общем
слое сцены: своего ввода, камеры и драга у него больше нет, геометрия и дроп идут по одному
дереву слотов, экраны — на канвасе. Ниже сохранён разбор того, ПОЧЕМУ первый заход был браком —
это единственная ценность, которая осталась в этом документе; порядок работ §3 выполнен целиком.

Что сделано (коммиты `7a59709`, `bce7657`):

| Шаг §3 | Чем закрыт |
|---|---|
| 0. shared/личное | `engine/sceneEngine.ts` (было сделано раньше, коммит `10ee73c`) |
| 1. Ввод | `SolitaireScene extends SceneEngine` — `InputRouter` из базы, своего разбора pointer нет |
| 2. Камера | камера базы; доска фиксированного размера, под экран её вписывает зум (issue #68) |
| 3. Драг | `SingleDrag`/`GroupDrag` из базы: подъём, пружина, тень, возврат домой |
| 4. Слоты | `game/slot/` — дерево, `dropTarget` по нему же, приём через `caps.drop.accept` |
| 5. Раскладка | `slot/layouts.pile` (каскад); `board/solitaireLayout.ts` и `solitaire/ui.ts` УДАЛЕНЫ |
| §4 экраны | `ui/TopBar` + новый общий `ui/OverlayPanel`; HTML в игре не осталось |
| §6 тест-кейсы | `e2e/solitaire.spec.ts` — все 12 |

Новые файлы: `solitaire/tree.ts` (+тесты), `solitaire/scene.ts`, `solitaire/slotPaint.ts`,
`ui/OverlayPanel.ts`. Гейты: `tsc` чист · `npm test` 538/538 · `vite build` · e2e **173/173**.

**Точка входа для следующего агента:** приёмка владельцем ветки `feat/solitaire-scene-engine`
(выкачена на `https://crusade-deck-client.fly.dev/v2/solitaire`), затем мердж в `main`.

---

## 1. Состояние кода на чекпоинте ПЕРВОГО ЗАХОДА (история)

Ветка `main`, **всё ЗАПУШЕНО** (`e6d995e`, сборка 464, прод отвечает). Первые шесть коммитов —
заход, который не приняли; дальше — начало переделки (см. §3/§4/§7). Откатывать не-принятое не
стали: по решению владельца оно переписывается на месте, а не ревертится.

| Коммит | Что |
|---|---|
| `eb2d7ab` | #68/#49 — единая система координат сцены, ресайз |
| `93a35f4` | отчёт за 2026-07-31 |
| `2503f64` | Косынка #92–#99 — первый заход |
| `b1a94ed` | отчёт за 2026-08-01 |
| `b6b3877` | Косынка — каскад, пружины, переворот |
| `d8b25be` | Косынка — центрирование карт, камера; `min-height:0` в общем CSS |
| `4a00480`+`59419f4` | этот документ: чекпоинт и порядок работ |
| `1400442` | peek: показ временный — тест приведён к коду (§7) |
| `10ee73c` | **общий слой сцены `SceneEngine`**, песочница переведена (§3) |
| `410bc38` | **топбар на канвасе** `ui/TopBar`, экранный слой сцены (§4) |
| `bdbfdc8` | этот документ: §3/§4 закрыты, список тест-кейсов |
| `e6d995e` | `test-results/` в `.gitignore` |

Гейты сейчас: `tsc --noEmit` чист · `npm test` 547/547 · `npx playwright test` **161/161**
(было 158/159; `peek.spec.ts:73` решён — §7, плюс два новых теста канвасного топбара).

### Что ЗДОРОВО и переписи не подлежит

- `board/solitaireDeck.ts`, `board/solitaireRules.ts`, `board/solitaireState.ts` — правила,
  колода, редьюсер, `faceUp`. E1.
- `solitaire/engine.ts` — `SolitaireGameEngine`, события `move`/`win`/`lose`. E2.
- `/no-ui` (`src/NoUi.tsx`) — играбельный дебаг-стенд чистой логики. **Ни одним коммитом не
  тронут**, работает как вчера. Это эталон «логика в порядке».
- Их тесты — 523 из 547 зелёных приходятся сюда.

### Что было НАПИСАНО МИМО — и чем заменено

| Было | Стало |
|---|---|
| `solitaire/app.ts` (329 строк) — свой pointer, своя камера, свой драг | `solitaire/scene.ts` поверх `SceneEngine`: ни строчки ввода и камеры (файл удалён) |
| `solitaire/ui.ts` (180 строк) — свой рендер слотов и расстановка карт | `solitaire/tree.ts` (дерево слотов) + `slotPaint.ts` (только черчение) (файл удалён) |
| `board/solitaireLayout.ts` — геометрия слотов заново | геометрия вытекает из дерева `game/slot/` (файл удалён) |
| `src/SolitaireGame.tsx` — экран на чужом CSS-классе | тонкий хост: канвас поднимается один раз, экраны рисует движок |

`solitaire/preset.ts` (`SOLITAIRE_BOARD_CONFIG`) — **удалён**: он дублировал `createInitialState()`
из `solitaireState.ts`, а дерево слотов строится от состояния партии, так что импортировать его
было некому. Задача #94, под которую он писался, закрыта как Done ещё до переписи.

---

## 2. Почему это брак — по фактам

В client2 есть слоёный переиспользуемый движок. Пасьянс не взял из него **ничего**:

| Слой | Общий модуль | Что сделал я вместо |
|---|---|---|
| Ввод | `engine/inputRouter.ts` | свой `pointerdown/move/up` |
| Камера | `engine/panZoom.ts` + `engine/viewport.ts` | своё применение трансформа |
| Драг | `engine/drag.ts` (`SingleDrag`/`GroupDrag`) | `body.snapTo` под пальцем |
| Слот и дроп | `game/slot/` (`Slot`, `dropTarget`, `caps.drop.accept`), `ui/DropZone.ts` | свои прямоугольники `slotGeometries` ОТДЕЛЬНО от рендера |
| Раскладка в слоте | `slot/layouts.ts` (`pile`, `linear`, `grid`) | свой `computeSlotCardLayout` |
| Правила как данные | `board/boardModel.ts`, `boardPresets.ts` | свой модуль геометрии |

Проверялось одной командой (файлов уже нет — проверка осталась как форма контроля):

```bash
grep -n "InputRouter\|SingleDrag\|GroupDrag\|SceneEngine\|dropTarget\|slot/" src/game/solitaire/*.ts
```

Тогда она находила **два совпадения, и оба — мой комментарий о том, почему я НЕ беру
`InputRouter`**. Сейчас находит реальные импорты общего слоя в `scene.ts` и `tree.ts`.

Наблюдаемые последствия, которые владелец и перечислил: жесты не такие, как в песочнице; драг
статичный, без пружины и подъёма карты; карты и зоны дропа живут в разных точках борда; зум
ведёт себя иначе.

**Причина ошибки (чтобы не повторить).** Я исполнял тексты тикетов буквально. Спеки #96/#97
диктуют `mountSolitaireBoard`/`updateBoardVisuals` и «повесь pointer-события» — я так и сделал.
Спеки переопределял только там, где вещи были *сломаны*, и не переопределял там, где они
*архитектурно неверны*. Доктрина client2 главнее текста тикета. Тикеты #96/#97 в этой части
считать недействительными.

---

## 3. Целевая архитектура

Требование владельца дословно: **пасьянс — standalone, полностью отвязан от логики песочницы;
всё переиспользуемое живёт в shared-модулях; копипасты нет.**

То есть: `solitaire/*` **не импортирует `playgroundEngine`** и вообще ничего «песочного». Общее
берётся из общих модулей. Если чего-то общего не хватает — оно выносится в общий модуль, а не
копируется.

Хорошая новость: фундамент уже написан и почти не используется (сейчас `game/slot/` тянут только
`board/stack.ts` и `board/field.ts`).

- `slot/layouts.ts` → `pile({dx: 0, dy: шаг})` — это и есть каскад колонки Клондайка.
  `linear({axis:"x", gap})` — верхний ряд.
- `slot/slot.ts` → `dropTarget(root, cp)` — попадание дропа **по тому же дереву, что и рендер**.
  Это и закрывает «карты и слоты в разных точках».
- `slot/types.ts` → `caps.drop.accept(figure)` — сюда заводятся правила Косынки из
  `solitaireRules.ts`. Зона сама решает, принимать ли карту.
- `slot/mutate.ts` → `detach`/`dropInto`.
- `engine/inputRouter.ts`, `engine/panZoom.ts`, `engine/drag.ts`, `ui/DropZone.ts` — как есть.

### Порядок работ — ВЫПОЛНЕН ЦЕЛИКОМ

Шаги 1–3 закрылись одним заходом, и иначе не могло быть: `SceneEngine` даёт ввод, камеру и драг
одним куском — сцена наследуется от него, а не «подключает» их по одному.

1. **Ввод** ✓ — `InputRouter` из базы. Своего разбора pointer-событий у пасьянса нет.
2. **Камера** ✓ — камера базы (пан/зум/пинч/колесо/инерция/клампы/авто-скролл). Не `attachPanZoom`:
   он для сцен БЕЗ своего движка, а сцена на `SceneEngine` получает камеру вместе с вводом.
3. **Драг** ✓ — `SingleDrag`, а пробег колонки — `GroupDrag`. Подъём, пружина, тень, возврат домой.
4. **Слоты** ✓ — дерево `game/slot/` в `solitaire/tree.ts`; приём через `caps.drop.accept` из
   `solitaireRules`; попадание дропа — `dropTarget` по ТОМУ ЖЕ дереву, что рисует карты.
5. **Раскладка** ✓ — `slot/layouts.pile({dx:0, dy:шаг})`; `board/solitaireLayout.ts` удалён.

Два решения, принятых по ходу и не описанных в исходном плане:

- **`pile` получил `cell`** — резерв места для ПУСТОЙ кучи. Пустая группа мерялась в 0×0, то есть
  колонка и фундамент пропадали как цель ровно тогда, когда в них и надо положить (король, туз).
  Правка в ОБЩЕМ модуле, а не обход в пасьянсе: этого не хватало самому слот-контракту.
- **Доска фиксированного размера, под экран её вписывает камера** (issue #68). Раньше геометрия
  пересчитывалась от вьюпорта тремя профилями — из-за этого размер карты скакал от ширины окна, а
  ресайз пересобирал всю сцену. Теперь ресайз только заново вписывает доску зумом.

### Разделение shared / личное в песочнице — СДЕЛАНО

**РЕШЕНИЕ ВЛАДЕЛЬЦА: этот кусок идёт ПЕРВЫМ, до шагов 1–5.** Не «разумно», а обязательно: если
переводить ввод пасьянса раньше, чем появится общий слой, пасьянс снова обрастёт своим — ровно
так первый заход и превратился в параллельную реализацию.

Выполнено (коммит `10ee73c`). Общая обвязка вынесена РЕФАКТОРИНГОМ из `playgroundEngine.ts`
(3047 строк) в **`engine/sceneEngine.ts`** — слой между тонким `CanvasApp` и конкретной сценой.
Песочница осталась зелёной по e2e.

В общем слое (`SceneEngine`):

| Что | Было в песочнице | Стало |
|---|---|---|
| Полотно и слои | `content`, `buildLayers`, `placeCard` | база |
| Камера | `syncVp`/`clampView`/`applyView`/`zoomAround`/`onWheel`/пан-инерция/пинч/`edgeScroll` | база |
| Ввод | `inputHandlers()` целиком, `hitElement`, `hitButton`, ховер | база + швы домена |
| Драг | `dragCtx`, `SingleDrag`, `releaseElement` | база |
| Дроп-зоны | `registerZone`, подсветка armed/hot, диспатч дропа | база |
| «Подглядеть» | `startPeek`/`endPeek`/`resolveGrabbedPeeks` | база |
| Цикл и тени | `frame`, `render`, проброс reduce-motion/вспышек/профиля | база |

Сцена реализует только своё — `draggables()`, `everyElement()`, `homeOf()` — и по желанию
переопределяет швы с рабочими умолчаниями: `pickElement`, `canDrag`, `beginDrag`,
`beforeDragMove`, `dragPoint`, `onDragMoved`, `beforeDrop`, `resolveDrop`, `onDragCancel`,
`afterDragEnd`, `onElementBlocked`, `flipGroup`, `stepScene`, `reapDead`, `renderScene`,
`layoutChrome`, `chromeInsetTop`. **Сцена без своих особенностей не пишет ни строчки ввода и
камеры** — этого и не хватало пасьянсу в первом заходе.

### Что осталось личным в playgroundEngine (объём на потом)

Владелец выбрал «минимум сейчас + план на остаток». Вынесено ровно то, на чём встанут шаги 1–5.
В `playgroundEngine.ts` (2586 строк) осталось личное стенда — выносить его в общий слой **не
нужно**, это не обвязка, а содержимое:

- разделы-витрины: `buildCardsRow` (сторис), `buildButtons`, `buildWidgetsBlock`,
  `buildDropIndicatorDemo`, `buildControls` с блоками flip/conceal/reveal/move;
- борды-примеры: `buildBoardZones`, `buildOneBoard`, `buildChessBoard`, `buildMixedBoard`,
  `buildCapabilityBoard`, `buildSelectDemo`;
- механики стендов: стопки (`buildStacks`), фишки/фигуры (`buildPieces`, `buildChipStack`),
  Поле (`buildField` — сама механика и так в `board/field.ts`), мультиселект (`selection.ts`).

Кандидаты на вынос ПОЗЖЕ, если понадобятся игре (сейчас спекулятивно — не выносить впрок):

1. **Метки-драггеры** (`Marker`/`grabbers`/`pendingHost`) — механизм «тянуть цель за метку».
   Пасьянсу не нужен; понадобится, если у игры появятся стопки, которые тянут за грип.
2. **Раскладка секций стенда** (`sectionFrame`, `sandboxLayout.ts`, `sandboxWrap.ts`) — это
   вёрстка СТЕНДА, не игры. Общей она станет, только если появится второй стенд.
3. **Порт команд доски** (публичное API «сервер двигает карты») — просится в общий слой, когда
   к пасьянсу/столу приедет сеть.

---

## 4. Обвязка экрана: НЕ CSS, а канвас

**РЕШЕНИЕ ВЛАДЕЛЬЦА отменяет прежнюю постановку этого раздела.** Разделять CSS не надо —
HTML тут не должно быть вовсе: **приложение целиком рисует движок**, включая топбар с кнопками
«назад»/«рестарт». Причина — переносимость на платформы без DOM. HTML остаётся по минимуму;
`/no-ui` и дев/тест-аффордансы — сознательные исключения.

Сделано (коммит `410bc38`):

- **`ui/TopBar.ts`** — общая канвасная панель из обычных `Button`: кнопки слева, статус справа,
  узкий экран гасит статус, но полосу НЕ переносит на вторую строку (иначе поле стола прыгало бы
  по высоте при каждой смене подписи). Своих pointer-событий не слушает — ввод ведёт движок.
- **`SceneEngine`**: слой `chrome` поверх контента и ВНЕ камеры (не панится, не зумится),
  `chromeButtons` с хит-тестом в ЭКРАННЫХ координатах, `layoutChrome` на ресайзе.
- **`InputRouter`**: необязательный `pickOverlay(sx, sy)` — HUD спрашивается ПЕРВЫМ, до карт:
  панель нарисована поверх сцены, и палец по кнопке не должен вместо неё хватать карту под ней.
- **Камера работает в подпрямоугольнике под HUD** (`chromeInsetTop`). Без этого верх стола
  навсегда заезжает под непрозрачную панель: кламп упирается в 0 и доскроллить нечем. Сцене без
  HUD инсет 0 — поведение прежнее.
- Песочница переведена: HTML-топбара нет. Скроллбары и слайдер зума ОСТАВЛЕНЫ сознательно — это
  аффорданс стенда для мыши, «исключение дев-назначения»; сдвинуты под полосу панели.
- Пасьянс перестал заимствовать класс `playground` (свой `solitaire-play`).

**Закрыто (коммит `bce7657`): HTML в «Косынке» не осталось совсем.**

- Топбар игры — общий `ui/TopBar` («← в меню» / «⟲ новая» / статус «Ходов: N»).
- Экраны фаз (меню / победа / поражение) — новый общий **`ui/OverlayPanel`**: затемнение,
  заголовок, подпись, кнопки. Сделан ОБЩИМ, а не «экранами Косынки»: экран фазы нужен любой
  игре, и второй такой был бы ровно тем копированием, из-за которого пасьянс переписывался.
- Панель живёт НИЖЕ топбара: затемнение гасит стол, а полоса управления остаётся яркой — иначе
  притушенная, но кликабельная кнопка врёт про своё состояние.
- Пока панель видна, `pickElement` возвращает null (карты не хватаются сквозь неё), а её кнопки
  уходят из `chromeButtons` при скрытии — невидимая кнопка иначе перехватывала бы тапы по столу.
- `SolitaireGame.tsx` стал тонким хостом: канвас поднимается один раз, ни фаз, ни экранов, ни
  топбара в React. Блок `.solitaire-*` и `.fd-btn-lg` из `theme.css` удалены.
- Эмодзи в заголовках убраны: цветной глиф ломает пиксельный шрифт (см. HANDOFF §1).

Дев-хук топбара: `__fd.testHooks().topbar` отдаёт экранные прямоугольники кнопок по ключу —
канвас не даёт ни DOM-узлов, ни ролей, и e2e кликает по ним.

Отдельно: правка `.table-host { min-height: 0; overflow: hidden }` в `d8b25be` — **общая и
правильная, её не откатывать**. Без неё flex-элемент не может стать ниже своего содержимого, и
канвас навсегда залипал в прежней высоте при уменьшении окна (баг был и в песочнице, и на столе;
ресайз из #49 наблюдателя завёл, но в этот тупик не упирался).

---

## 5. Ловушки, уже найденные — не наступать заново

1. **Спеки issue местами описывают проект, которого нет.** Ссылаются на
   `client2/src/FreeDesk.tsx` (остался в старом `client/`) и говорят о 14 слотах при фактических
   13 (сток + сброс + 4 фундамента + 7 колонок). Сверяться с кодом.
2. **`layout:"fan"` для колонок из #92 — ошибка спеки.** Веер уводил карты по дуге ВВЕРХ, на
   верхний ряд: клик по стоку попадал в наехавшую карту колонки, дроп не находил колонку. Канон
   Клондайка и решение владельца — вертикальный каскад.
3. **Позиция карты — это её ЦЕНТР**, а слот задан прямоугольником от левого верхнего угла. Без
   перевода карты стоят на полкарты выше и левее зоны, и дроп считается не туда. В общем
   слот-дереве этой ловушки нет — там одна система координат.
4. **`Card.faceUp` присваиванием не перерисовывает карту.** Текстуру кладёт приватный `paint()`
   из шага анимации. Открывать карту только через `requestFlip()`.
5. **Шагать карту надо `card.step(dt)`, а не `card.body.step(dt)`.** Второе тихо выключает
   собственные анимации карты (переворот, «стоп»-кивок, горение).
6. **`engine.resetGame()` ничего не эмиттит** — шина знает только `move`/`win`/`lose`. Доска
   останется от прошлой партии. Явный путь — `SolitaireScene.newGame()`: он сносит все визуалы и
   пересобирает доску (переворачивать 52 карты анимацией — это не сдача, а фокус).
7. **`frame()` обязан шагать анимации и возвращать «занято»**, иначе тикер спит и всё выглядит
   телепортом. На `SceneEngine` это идёт из базы — своего `frame()` сцена не пишет.
8. **Найдено при переписи: пустая группа слотов мерялась в 0×0.** Колонка и фундамент пропадали
   как цель дропа ровно тогда, когда в них и надо положить (король, туз). Лечится `cell` у
   `pile()` — резервом места. Правка в ОБЩЕМ модуле: этого не хватало слот-контракту, а не
   пасьянсу.
9. **Найдено при переписи: `newGame()` может быть вызван ДО `mount()`.** Хост так и делает; без
   гварда на несобранную сцену `new Card(...)` падал на `tex === null`. Гейты этого не видят —
   ловится только запуском в браузере.
10. **Карту в каскаде надо хватать за ВИДИМУЮ полоску, а не за центр.** Центр закрытой картой
    перекрыт следующей, и хит-тест справедливо отдаёт верхнюю. Это не баг, но на этом спотыкается
    любой тест, который целится в центр карты (см. `grabPoint` в `e2e/solitaire.spec.ts`).

---

## 6. Как проверять, чтобы не позориться

**Зелёные гейты здесь не значат ничего.** Pixi в node не исполняется: `tsc`, `npm test` и
`npm run build` не видят визуальный слой в принципе. Так уже прошли мимо `baseScale=1` (карта
втрое больше слота) и смещение на полкарты.

Отдельно про дырявые проверки — на этом я обжёгся лично: **тест «нелегальный дроп ничего не
изменил» даёт тот же результат, что и полностью неработающий драг.** Он не доказывает ничего.

Обязательный минимум ручной проверки (dev-сервер на 5174, `/solitaire`):

1. Взять легальный ход из самого движка: `engine.getPossibleMoves()`.
2. Выполнить его **мышью**, а не вызовом движка.
3. Доказать, что доска **изменилась**: карта в целевом слоте, `movesCount` вырос.
4. Проверить, что карта под снятой **открылась И рисуется лицом** (сверять `engine.isFaceUp`
   с `node.faceUp`).
5. Тап по стоку: сток −1, сброс +1.
6. Ресайз окна и узкое окно: доска видна целиком.
7. Жесты сверить с `/playground` бок о бок — они обязаны совпадать.

Весь этот список пройден руками на переписи — см. тело коммита `7a59709`.

Дев-хук `window.__sol` (в `SolitaireGame.tsx`, только DEV) заведён именно для этого — канвас не
отдаёт ни DOM-узлов, ни ролей. Сохранён и дорос: `SolitaireScene.testHooks()` отдаёт экранные
прямоугольники слотов, позиции карт с `faceUp`/`scale`/`rot`/`state`, кнопки топбара и экрана
фазы и **экранный размер карты, считанный от её `baseScale`** — только так тест ловит
`baseScale=1`. Аналогично `window.__menu` в меню и `window.__fd` в песочнице.

Экраны победы и поражения **посмотрены глазами** (раньше их не видел никто): партию до конца не
доиграть, поэтому фаза подменялась в движке напрямую — проверялся ВИД панели. Логику эмиссии
`win`/`lose` держат юнит-тесты E2.

### Обязательные тест-кейсы на найденные баги — НАПИСАНЫ

`client2/e2e/solitaire.spec.ts`, все 12, зелёные. Каждый баг, который владелец ловил руками, стал
кейсом — иначе он вернётся. Список собран из §2 (наблюдаемые последствия) и §5 (ловушки).

| # | Сценарий бага | Что должен доказывать тест |
|---|---|---|
| 1 | Жесты не как в песочнице | Пан/зум/пинч/инерция на `/solitaire` дают тот же результат, что и на `/playground` при том же жесте |
| 2 | Драг статичный: без пружины и подъёма | Схваченная карта поднялась (scale > 1, есть тень) и едет за пальцем; отпущенная мимо — возвращается пружиной, а не телепортом |
| 3 | Карты и зоны дропа в разных точках | Легальный ход из `engine.getPossibleMoves()`, выполненный МЫШЬЮ в центр целевого слота, принимается; доска изменилась и `movesCount` вырос |
| 4 | Зум ведёт себя иначе | Колесо с Ctrl/Cmd зумит, без модификатора панит — как в песочнице |
| 5 | Смещение на полкарты (§5.3) | Экранный центр карты в слоте совпадает с центром зоны слота |
| 6 | `baseScale=1`: карта втрое больше слота (§6) | Карта помещается в свой слот: её ширина ≤ ширины слота |
| 7 | `faceUp` присваиванием не перерисовывает (§5.4) | Карта под снятой открылась И РИСУЕТСЯ лицом: `engine.isFaceUp` совпадает с `node.faceUp` |
| 8 | `resetGame()` ничего не эмиттит (§5.6) | «Новая игра» реально перестраивает доску, а не оставляет прошлую партию |
| 9 | Телепорт вместо анимации (§5.7) | После хода есть кадры, где карта в пути (позиция между старым и новым слотом) |
| 10 | Веер вместо каскада (§5.2) | Карты колонки идут вертикально вниз; клик по стоку попадает в сток, а не в наехавшую карту колонки |
| 11 | Сток/сброс | Тап по стоку: сток −1, сброс +1 |
| 12 | Узкое окно и ресайз | Доска видна целиком, канвас следует за размером окна |

Отдельно про дырявые проверки: тест «нелегальный дроп ничего не изменил» в этот список НЕ входит —
он даёт тот же результат, что и полностью неработающий драг, и не доказывает ничего (см. выше).

---

## 7. Открытые вопросы к владельцу

- ~~**`peek.spec.ts:73`**~~ — **РЕШЕНО владельцем: прав КОД.** Показ «подглядеть» всегда
  ВРЕМЕННЫЙ: перехватить раскрытую карту повторным драгом можно, но «выиграть» ею скрытность
  нельзя — по концу драга карта возвращается в исходный вид сама. Тест ждал обратного и приведён
  к коду (`1400442`); гейт e2e снова полный.
- **#101 BoardFactory v2** — создан, без приоритета. Смыкается с §3: «гриды настраиваемые от
  контента» — это ровно то, что делает `slot/layouts.grid`.
- ~~**`test-results/`**~~ — **сделано**, в `.gitignore` (`e6d995e`).
- ~~**Пуш**~~ — **сделано**, владелец дал «ОК». Всё в `origin/main`, прод выкачен (сборка 464).
  Первая выкатка упала не по нашей вине — удалённый сборщик Fly (depot.dev) не ответил по
  таймауту; перезапуск упавшего джоба прошёл. Тестовые джобы были зелёные с первого раза.

### Ещё открыто

- **Пустая треть внизу на телефоне.** Доска фиксированного размера и почти квадратная (924×856),
  а телефон узкий — вписывание упирается в ШИРИНУ, и под доской остаётся пустое поле. Варианты:
  оставить как есть, центрировать по вертикали, или дать доске тянуться в высоту. Ждёт владельца.
- **Мёртвые экспорты в ОБЩИХ board-модулях**: `container.runFrom`, `board.isEmpty`,
  `board.occupiedKeys` — ни одного потребителя, кроме собственных тестов. Не тронуты сознательно:
  это общий слот/борд-контракт, а не пасьянс, и чистить его надо отдельным заходом по всему
  `game/board/`, а не походя.
- **#101 BoardFactory v2** — создан, без приоритета. Смыкается с §3: «гриды настраиваемые от
  контента» — это ровно то, что делает `slot/layouts.grid`.

## 8. Состояние борда

Done (здоровая логика и закрытое ранее): #75, #76, #83–#91, #100, #94, #95, #99, #82, #49, #68.

Сделаны переписью и ждут приёмки: **#92, #93, #96, #97, #98**. Эпики **#77, #78, #80** остаются в
In Progress — их статус за владельцем: все сабтаски закрыты, но #77 владеет файлом
(`board/solitaireLayout.ts`), которого больше нет, а «14 слотов» в его Goal — ошибка спеки
(слотов 13).

**Точка входа для следующего агента: приёмка ветки `feat/solitaire-scene-engine`** (выкачена на
`https://crusade-deck-client.fly.dev/v2/solitaire`) и мердж в `main`. Ветка выкачена НА ТУ ЖЕ
аппу Fly, что и прод: любой пуш в `main` запустит CI и перезатрёт её кодом `main`.
