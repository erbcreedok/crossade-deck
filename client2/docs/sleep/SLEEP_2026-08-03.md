# Ночной прогон 2026-08-03: merge storybook → main, игра crossade

Задание владельца: влить сторибук в мастер; на ветке `crossade` собрать в client2 ту же игру,
что живёт в client/ (мультиплеерный стол на Colyseus), отдельной папкой по образцу Косынки,
подключить к текущему серверу; база — только client2, client/ — макет-референс.

## Сделано

| шаг | коммит | что |
|---|---|---|
| merge | `e8b78d6` | storybook → main (--no-ff), проверено: client2 778/778, server 243/243, tsc чист. **Запушено** (явное «надо вмержить» владельца); CI задеплоит main |
| ветка | — | `crossade` от свежего main, запушена пустой |
| контракт | `9924bf8` | `docs/CROSSADE-DESIGN.md` — структура, этапы, границы MVP |
| цикл 1 | `60079c3` | `src/net/` — адреса (цепочка конфигов), аккаунт (localStorage + API), подключение с ретраем. 30 юнитов |
| цикл 2 | `1027e99` | `crossade/state.ts` (снимок, stale-check по deckRev, удержание порядка руки) + `tree.ts` (дерево слотов). 28 юнитов |
| цикл 3 | `6472091` | `crossade/net.ts` — схема→снимок duck-typing'ом, порт 19 команд (имена сверены с сервером 1:1), сигналы. 24 юнита |
| цикл 4 | `b1095ff` | сцена + канвасное лобби + роут `/crossade` + пункт в меню. Аккаунт создаётся сам при первом входе. **Проверено против живого сервера руками** |
| цикл 5 | `d53a5c0` | раздача драгом на место игрока (дилер, лобби), реордер руки чистой функцией, подсветка мест под драгом. **Проверено против живого сервера** |
| цикл 6 | `76542d1` | e2e: свой игровой сервер на 2678 в playwright.config + 5 сценариев через `__cro` |

## Что работает (всё проверено против настоящего сервера, не по юнитам)

Лобби (канвас): создать стол / тестовый стол с ботами / назад. Стол: колода в центре (лобби) и
слева (игра), сброс, play-зона, своя рука, полоса мест со счётом карт. Дилер: раздача драгом на
место (боту и себе), «ГОУ!», «раздать всё», «перераздача». Игрок: готов/не готов, тап по колоде
(взять), драги рука↔сброс↔стол, реордер руки. Отказы сервера — notice на канвасе.

## Цифры

- Юниты client2: **882/882** (было 778 — +104 за ночь). Сервер: 243/243. tsc чист, vite build проходит.
- e2e: **187/187** (177 app + 5 crossade + 5 catalog).

## Ловушки ночи (чтобы не наступать again)

1. **`.env.local` целится в LAN IP** (тесты с телефона). Пока адрес недоступен, `POST /accounts`
   висел системные ~100с и лобби выглядело зависшим. Дал ручкам аккаунта потолок 8с
   (`AbortSignal.timeout`); e2e-конфиг целится в свой сервер через env поверх `.env.local`.
2. **pkill по «vite.*5174» не убивает vite** — порта нет в командной строке. Убивать по
   `lsof -ti :5174`.
3. **Протухший сторибук после переключения веток** отдаёт битую сборку: пять catalog-тестов упали
   таймаутом. Свежий инстанс — зелёные за 20с. Перед e2e перезапускать давно живущие dev-серверы.
4. Один субагент вернул «жду фонового прогона» вместо результата — прогон добит координатором
   лично (правило скилла «verify сам» сработало как задумано).

## Не пушено

Коммиты ветки `crossade` (контракт + 6 циклов) — локальные, ждут «ОК» владельца.
`origin/crossade` существует, но пуст (создана от main).

## Осталось / решения владельца

- **Восстановление аккаунта по коду** — канвасного ввода текста в client2 нет; экран отложен
  (см. CROSSADE-DESIGN.md). Recovery-код пока нигде не показывается — решить, где (меню?).
- Вторая очередь по макету: голосования, кричалки, свайп-тасовка, веера колоды/сброса,
  авто-раздача, П-рассадка с боковыми соседями (сейчас все места полосой сверху),
  discard-горочка с фиксированным узором, паблик-список комнат, вход по коду.
- Топбар на узком экране: «Комната NNNN · за столом…» подрезается кнопками — сверстать теснее.

## Как тестировать руками

```bash
cd server && npm run dev          # :2567
cd client2 && npm run dev         # :5174 (если .env.local целится в LAN — поправить или задать
                                  #  VITE_SERVER_URL/VITE_HTTP_URL=localhost:2567 при запуске)
# браузер: http://localhost:5174/crossade → «тестовый стол (боты)»
```
e2e: `cd client2 && npx playwright test e2e/crossade.spec.ts --project=app` — сервер поднимется сам на 2678.
