# 🌙 me-sleep — сессия 01 (ночь 2026-07-30)

**Цель:** добить колонку **In Progress → Ready for test** (client2). Достигнута:
колонка In Progress пуста.

## Сделано

| # | Задача | Приоритет | Коммит | Статус |
|---|--------|-----------|--------|--------|
| #3 | Скрытая карта на /table (dust + reduce-motion fallback) | p-high | `c8ab647` | Ready for test |
| #6 | Тряска рядов: интенсивность/скорость настраиваемые | p-low | `b866c5e` | Ready for test |
| #12 | /motion: секции флип / сжечь / idle | p-low | `95c55c8` | Ready for test |

Все коммиты **локальные, НЕ запушены** (по правилу скилла — пуш только по твоему ОК).
База отката: `257304c` (состояние до сессии). Откат любого цикла — `git reset --hard <hash>`.

## Как тестировать (кратко)

`cd client2 && npm run dev`, затем:
- **#3** — `/table`: понятие hidden только на уровне движка (`TableEngine.setHidden(card,true)` из devtools-консоли) → живая пыль как на `/free-desk`; reduce-motion/reduced → статичный `hiddenFace`. UI-кнопки на странице нет (вне скоупа §B.4).
- **#6** — `/motion` → секция «Тряска рядов»: новые слайдеры «интенсивность»/«скорость». 100%/100% = старое поведение.
- **#12** — `/motion` ниже «Тряски рядов»: «Флип карты» (кнопка «перевернуть»), «Сжечь» (красная кнопка), «Idle-дыхание (заглушка)».

## Требует внимания владельца

1. **#12 idle-дыхание — заглушка, а не эффект.** В движке idle-дыхания нет: есть
   только неиспользуемый конфиг `ANIM.idle` (`anim/config.ts`), нигде не читается.
   Секция стенда — текстовый TODO. Флип и сжечь — полноценные. Если issue #12 требовал
   реального idle-эффекта — это отдельная задача (применить `ANIM.idle` в `Card.step()`),
   заведи тикет.
2. **#3 без демо-контрола.** hidden введён на уровне движка; клика «скрыть» прямо на
   `/table` нет (вне скоупа). Если нужен для ручного QA — follow-up.
3. **Пред-существующие красные e2e (НЕ от этой сессии).** На чистом main падают 3 спеки:
   `visual.spec.ts:46` (снапшот `hidden-face-darwin.png`), `peek.spec.ts:73`,
   `pieces.spec.ts:37` — визуал-снапшоты, расходятся до наших правок (проверено stash'ем).
   Стоит пересобрать снапшоты (`--update-snapshots`) на этой машине или разобрать отдельно.

## Токены / замечания процесса

- #3 и #6 гонялись параллельно в ОДНОМ рабочем дереве — файлы не пересеклись, закоммичены
  раздельно. #12 запускал последовательно (правит тот же стенд `/motion`, что и #6).
- Верификация делалась лично (`npm test` 446/446, `tsc` clean) — не на доверии к агентам.

## Осталось в In Progress

Пусто. Следующая работа — по твоему выбору (баги эпика #58 из `open-tasks.md §F`,
либо новые задачи с борда).
