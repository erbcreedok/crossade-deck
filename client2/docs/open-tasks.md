# Открытые задачи и планы — client2 (зафиксировано «как есть»)

Спутник [`HANDOFF.md`](./HANDOFF.md). Здесь — незакрытые хвосты, отложенные решения и планы.
Ничего не отсеивалось (решение владельца: «зафиксировать всё как есть»). Разбор keep/drop —
за новым владельцем сессии.

## A. Отложенные решения (ждут выбора)

1. **Канвас-слайдер vs Stepper.** Числовые контролы на `/motion` — это `Stepper` (−/+, целые
   шаги), т.к. канвас-примитива «плавный слайдер» нет. Из-за этого дробные шаги (напр. дрожание
   0.1) огрубились до целых. Открыто: сделать ли отдельный UI-примитив «слайдер» в канвасе.
2. ~~**Какой ОДИН вариант цензуры идёт в прод.**~~ РЕШЕНО: «TG-пыль» (частицы) — дефолтный вид
   скрытой карты. Дефолт-рычаги (частица 5 / свапы 25 / дрожание 1 / частота 1, мерцание выкл,
   пыль замедлена ×3) вынесены в `censorConfig.ts` — один источник правды для стенда и доски.
   Остальные 3 (CPU-мозаика, GPU-remap, GPU-pingpong) остаются на стенде как «анимации фигур для
   других кейсов» / dev-сравнение.
3. **Плотность частиц как рычаг.** Реальная цена частиц — их КОЛИЧЕСТВО (не size/freq). Сейчас
   зашито константами (`step=4`, `perCell=2` в `censorDemo.particleCard`). Предлагался ползунок
   «плотность» с пересборкой облака — не сделан.

## B. Предложено, но не построено

4. **Production-интеграция.** ЧАСТИЧНО СДЕЛАНО: живая «пыль» подключена к `ui/Card` (скрытая
   карта = чистый фон `hiddenBg` + оверлей `ParticleField`; крутится в `Card.step`, держит цикл
   бодрым через `resting=false`, прячется при перевороте). Видно на `/playground` («скрытая
   (пыль)»). ОСТАЁТСЯ: `/table` (`TableEngine`/`CardVisual`) вообще не знает про `hidden` — там
   сперва нужно ввести само понятие скрытой карты, потом навесить тот же оверлей. Плюс привязка
   к профилю качества/reduce-motion (тогда fallback — статичный `hiddenFace`).
5. ~~**«Тряска рядов» тюнингуется минимально**~~ ✓ issue #6: `CensorSpec.intensity` (новое поле,
   множитель амплитуды motion+bias, дефолт 1 = старое поведение) + `speedPxSec` (уже было полем
   spec, просто не крутился UI) — оба живые слайдеры «интенсивность»/«скорость» 0–200% в секции
   «Тряска рядов» на `/motion`, рядом с тумблером «двигать». Guard-тест на дефолт в
   `censorMotion.test.ts`.

## C. Из `animation-levers.md`, что ещё НЕ реализовано (чеклист §4)

6. **Master reduce-motion, читающий ОС** (`prefers-reduced-motion` / iOS/Android). Сейчас тумблер
   `/motion` есть, но он локальный и не читает системную настройку.
7. ~~**Авто-профиль по FPS** — автопонижение качества при просадке.~~ ✓ issue #8: `FpsMeter`+`nextTier`
   (гистерезис 45/55) на базе `CanvasApp`, тир `full`↔`reduced`; `reduced` замораживает idle + гасит
   shadow-пасс; оверрайд `auto/full/reduced`; индикатор + dev-нагрузка на `/motion`.
8. ~~**Глобальный флаг вспышек/мерцания** (фото-чувствительность)~~ ✓ issue #9: `reduceFlash` на базе
   `CanvasApp`, `flashOff = reduceMotion || reduceFlash`; гасит мерцание частиц + дрожь «сжечь»; тумблер
   «без вспышек» на `/motion` (auto наследует `prefers-reduced-motion`).
9. **Драг-режим = глушить анимацию во время драга** («виснет драг» на слабых).
10. **Батарея/термал-aware** — резать idle/частицы на low battery (мобайл).

## D. Ещё не представлено на стенде как тест-кейсы

11. Idle-анимации (дыхание), «сжечь», флип карты/стопки — есть в движке/планах, но на `/motion`
    отдельными секциями-тесткейсами не заведены. Стенд задуман расширяемым под них.

## E. Заметки по цепочке прерванных задач (контекст)

- Была итерация раскладки `/motion`, где агент (Haiku) по ошибке удалил ВСЕ пресеты; откатили
  `git reset` к `341042a`, переделали верно. В истории коммитов этого следа нет (сброшен).
- Много ранних задач этой линии ЗАВЕРШЕНЫ (SVG-масти, лицо скрытой карты, шрифт Handjet,
  стенд, GPU-варианты, частицы, перевод `/motion` в канвас) — см. git-лог и §1 HANDOFF.
- Переход на новую сессию — из-за роста истории/токенов, а не из-за нерешаемой задачи.

## Рабочие заметки (взаимодействие)

- Модель пошаговая: входящие сообщения не видны, пока идёт проход; прерывание — штатный и
  правильный способ вклиниться (отменяет текущий проход, отдаёт новое сообщение).
- Владелец не любит простыни и оверинжиниринг; суть/действие вперёд, сверять на реальных
  паттернах, а не выдумывать.

## F. Баги эпика #58 (мультиселект) — фикс-сессия позже

**Статус на 2026-07-30:** эпик #58 (гибкий мультиселект) закрыт по разработке и принят локально
владельцем «пока сойдёт». Дети #70–#74 в Done. НО владелец отметил: **багов при локальном тесте
много** — их не диктовали, отложили на отдельную фикс-сессию.

**Что войдёт в контекст фикс-сессии:**
- Приёмка была быстрой («потестил локально, сойдёт») — конкретный список багов ещё НЕ собран;
  первый шаг сессии — владелец диктует баги (или прогон агентов на поиск по `/playground`).
- Затронутая механика: сборка набора (`gatherOn × anchor × order/sort`), пресеты
  `drag-start/follow-first/follow-last`, дроп-цепочка `resolveDropChain`, слепые зоны §6.
- Ключевые файлы: `src/game/board/assembly.ts`, `board/boardZone.ts`, `board/dropPolicy.ts`,
  `engine/playgroundEngine.ts`, `ui/Button.ts`; e2e `selection.spec.ts`, `capabilityZone.spec.ts`.
- Источник правды по механике: `client2/SELECTION-DESIGN.md` (§4–§7).

**Процессная заметка (важно для след. сессии):** при реализации #74 несколько агентов-исполнителей
зациклились на взаимном делегировании вместо работы («жду отчёта»). Дежурный вывод: давать
исполнителю ЯВНЫЙ запрет спавнить под-агентов, и координатору проверять дерево/тесты самому.
