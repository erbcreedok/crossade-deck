# Хендофф: кампания атомарности + дроп-механики (2026-08-05)

Состояние на момент передачи: `main` = `25d3e39`, всё запушено. tsc чист, клиент **1091 тест**,
сервер **250** — зелёные. Дев-серверы: клиент :5174, сервер :2567, сторибук :6006.

## Что сделано в последних сессиях (по коммитам)

- `3ee0214` — Card/Piece: общие модули способностей (elementLife, cardVeil, cardSecrecy, cardFace,
  elementGlow, itemFrame).
- `94ba9e6` — **Единый TableItem**: карта/фишка/фигура — один класс, виды через реестр
  (`ui/cardKind.ts`, `ui/builtKind.ts`, `ui/pieceKinds.ts`); способности — ДАННЫЕ (`caps`),
  capabilities.ts читает флаги, не наличие методов. `ui/Card.ts` и `ui/Piece.ts` — тонкие шимы.
- `20f872d` — дроп считает **фигура, а не палец** (`engine/dropPick.ts` для движка/косынки;
  `slot/slot.ts dropTargetRect` для деревьев); зона колонки косынки — весь каскад + запас.
- `25d3e39` — **дроп-политики зон** (`slot/dropPolicy.ts`): hit `overlap`/`center`/`finger`,
  `only` (вид груза), `maxTilt`, `magnet`. Политики — данные спеки
  (`boards/library/roundTable.ts`): колода — overlap+card+30°+магнит; центр — по центру карты;
  свободные стопки — палец. Сторибук: `Mechanics/Drop mechanics` с рычагами.
  Сторожа: `slot/dropPolicy.test.ts` (9), `boards/geometry/dropPolicies.test.ts` (6).

Решение владельца, зафиксированное в коде: наклонённая карта, впихнутая в колоду пальцем,
СОХРАНЯЕТ поворот (`fx.rot` не сбрасывается) — «механика такая». Нормализация при входе в
колоду, если попросят, — одна строка в `boards/core/mock.ts` (обработчик `move`/`cardFx`).

## Очередь работ «файлы 300+» (брать по порядку, команда владельца — «дальше»)

1. **Близнецы** `multiplayer/scene.ts` и `crossade/scene.ts` (2×596) → общая база + коллабораторы
   chrome/decor/pending/seatLabels по рецепту борды (`boards/scene/parts.ts`).
2. `engine/kitScene.ts` (880) → разрез kitBuildCtx / kitSpread / kitHooks.
3. `boards/scene/scene.ts` (~565, подросла на дроп-политиках) и `solitaire/scene.ts` (~430) —
   дорезать коллабораторами.
4. `ui/Button.ts` (356) — общий glow/тень; `NoUi.tsx` (420), stories (Boards 418,
   StackInteractions 348, stackArgs 310), `localTable` (352), `stackInteraction` (369),
   `cardTextures` (374). `inputRouter` (334) — допустимо оставить.

Отложено ранее: приватность рук в live.

## Каноны владельца (нарушение = переделка)

- Файлы <300 строк; композиция, не наследование; данные-политики вместо if-по-типам.
- Каждый шаг: tsc + полный vitest + браузерный смоук + коммит с цифрами + пуш.
- Правило владельца закрывать тестом СРАЗУ (см. колонку «сторож» в `catalog-rules.md`).
- «Готово» — только после прогона, с цифрами. Неоднозначный фидбек — переспрашивать.
- `client2/.env.local` (LAN IP) НЕ коммитить — стейджить только `src`.

## Ловушки, найденные в работе (не переоткрывать)

- Тикер движка спит: прямые мутации элементов из консоли/e2e требуют `rt.wake()`.
- Плейрайт-скриншоты падают в `/Users/giyers/`, не в cwd.
- Песочница в e2e: `window.__sandbox` (BoardScene), `testHooks()` — экранные координаты;
  `nodeStore.get(id).body.px` — контентные. Зеро-дистанс драг (карта уже в точке дропа) даёт
  ложный «провал» теста — сначала переставить карту.
- `board:N` — свободные стопки в круге, `board:0` — колода, `table:0` — центр. Дроп мимо круга
  (760×760, форма circle) → карта летит домой, это норма.
- Магнит читает спринг-таргет (`CardBody.targetX/Y`), не `px` — иначе залипает навсегда.
- vitest-фильтр по пути иногда «не находит файлов» — гонять `npm test` целиком.
