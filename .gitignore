node_modules
dist/
build/

# Локальные данные и настройки — не для коммита
server/data/
**/.env*
!**/.env.example
.claude/settings.local.json

# ОС/редакторы
.DS_Store
*.log

# Отчёты тестировщика — локальные
todo/

# Артефакты Playwright MCP (скриншоты/логи браузера из отладочных сессий)
.playwright-mcp/

# Артефакты прогона Playwright (скриншоты/трейсы упавших тестов) — производные, пересоздаются
test-results/

# оркестрация: задача и отчёт в полёте, история — в коммитах
.agent/NEXT.md
.agent/REPORT.md
.agent/tmp/
.agent/queue/
# Воркtree агентов-исполнителей — scripts/agents
.worktrees/
# Секреты сервера и бота — никогда в git
server/.env
server/.env.table
bot/.env
bot/data/

# Снимки экрана от прогонов — мусор, а не исходник.
server/*.png
