{
  "docs.tests.node": [
    "This section is the catalog CHECKING ITSELF, rung by rung. Open a story and its checks run right away — opening the page is the request. Each step ticks past in the Interactions panel below the canvas (or behind the drawer button on a phone), and a red step names the claim that broke, not just the story.",
    "The steps make the one measurement no headless layer can: they read pixels off a real WebGL canvas. \"The plan says draw a square\" and \"a square is on the glass\" have been different statements in this project before.",
    "Automation opens the same stories and asserts on the same steps — one body, two consumers, no second copy to keep in step."
  ],
  "docs.tests.node.growth": "The phone-reload case, replayed on demand. A page still laying itself out measures a scene at next to nothing; the renderer starts asynchronously; the real size arrives inside that start window — and a renderer that has not started cannot take a resize. The step builds a second scene of its own in a two-pixel box, grows the box in the same breath, waits the scene out and reads the glass: the card must be there at FULL size. Before the fix this was the canvas that came up empty on a phone until any toggle nudged it.",
  "docs.tests.node.lifecycle": "The first rung, and the one the rest stand on: a node is there, it goes, it comes back. The scene is fed a tree without the card and then one with it again — a new tree is new DATA, not a new showcase, so the canvas underneath never changes and no second WebGL context is spent. The last step runs twenty rounds and compares the first pixel to the final one: a renderer that ACCUMULATES looks right on frame one and wrong on frame twenty.",
  "docs.tests.bounded": [
    "The second rung: the box. Bounded is the one atom whose whole lesson is invisible — it declares room and paints nothing — so every claim here is made through the debug outline, the same way the Atoms/Bounded shelf shows it.",
    "The measurements are whole-glass, not point probes: an outline is a stroke a couple of device pixels wide, and a check that samples points either misses it or has to know where the stroke runs. The steps read the full buffer and answer with a count of inked pixels and their bounding box — a size claim is a claim about that box."
  ],
  "docs.tests.bounded.outline": "Four claims on one canvas: the outline is the ONLY ink (the middle of the box still reads as desk — Bounded has no fill); a wider rect is a wider outline and no taller; square, circle, star and a hand-written path each leave a different pattern on the glass; and a node without Bounded leaves it bare — zero pixels, not merely fewer.",
  "docs.tests.surfaced": [
    "The third rung: the paint. The rung below proves the box is real and invisible; this one is about the atom that finally draws, and about the seam neither rung below can reach — the AREA a surface is painted onto is not always the box a reader assumes.",
    "The debug outline stays OFF here, unlike the Bounded rung: the steps measure what the SURFACE put down, and a hairline from another layer sits in the same buffer and would be counted as paint. Sizes are checked against arithmetic rather than a stored number — one unit is worth so many pixels at this etalon, so \"the fill is 1.6 units wide\" is exact on any screen."
  ],
  "docs.tests.surfaced.paint": "Eight claims on one canvas. The fill covers exactly the box that was declared; a box without the atom and the atom without an area both leave the glass bare; re-registering the record repaints the node and moves no box; a record with no layers still draws its rim, in its own colour; a thicker rim inks more and a dashed one inks less, because width and dash are absolute lengths rather than a picture stretched to fit; plate, bare and zone are three different pictures; the paint may reach PAST the box (an outside-aligned border) or fall SHORT of it (a contained picture); and a desk with no box of its own takes its area from what it holds, shrinking when a child is taken away.",
  "docs.tests.surfaced.area": "Four claims about the OTHER area. `Surfaced` accepts a box or the extent of what a node holds, and this page takes the second branch apart: a container holding nothing paints nothing, and it is not a latch — one box brings the area back; children that carry no boxes reserve nothing, so plenty of content is still no area; a node with a box of its own paints THAT, even when it is smaller than what it holds, because the derived area is the fallback and not the sum; and the same two boxes in a row and in a free layout paint different areas, because the layout is what decided where they went.",
  "docs.tests.transformable": [
    "The fourth rung: the pose. The rung below proves the paint is real; this one is about WHERE it lands when a node says `at`, `z`, `angle` or `scale` — and what those fields do along a chain of owners, measured on the glass rather than read off the resolve arithmetic the unit suite already trusts.",
    "Position and size claims are checked against arithmetic — one unit is so many pixels at this etalon, and a card with no pose sits on the canvas's own middle — so \"the card moved half a unit\" is an exact number of pixels on any screen. Where two programs must make the SAME picture (a chain against its sum), the two frames are compared pixel for pixel, with a different frame between them so each reading is known to have arrived."
  ],
  "docs.tests.transformable.motion": "Five claims about one node's own pose. Half a unit of `at` is half a unit of pixels of travel and not a hair of size; scale-then-turn-then-move is pinned by a doubled card whose middle stays at 0.6 units — moved-then-scaled it would sit at 1.2; a quarter turn on a box with two different sides swaps the ink's width and height exactly, with no float smear at the right angles; a full lap changes nothing (390° is 30°, −90° is 270°); and scale works both axes at once, paints nothing at zero without crashing, and comes back the moment the zero is gone.",
  "docs.tests.transformable.chain": "Five claims about the chain. A hand's 30° and its card's 15° make the same picture as a single 45° — two programs, one picture, compared pixel for pixel; a card offset inside a turned hand ends BELOW it, because clockwise is what the screen's y calls down and the pivot is the OWNER's origin, not the desk's middle; a half inside a half shows a quarter, and a silent child is simply carried; the same two poses under `free` and `row` span different pictures, because a layout writes `at` and a free one writes nobody's; and one card object reparented from a half-size hand to a full-size one is full size on the next frame — the chain is read at apply, never frozen.",
  "docs.tests.transformable.order": "Three claims about the field that is not geometry. Lifting one of two overlapping plates flips which colour the overlap shows while the union of ink does not move a pixel — a lift is not a distance; a card that says nothing about height covers a lifted loose card when its PILE stands higher, and lowering the pile alone hands the cover back — `z` is the sum along the chain; and at equal height tree order decides, swaps when the additions swap, and holds on every redraw — an unstable tie is cards flickering between frames.",
  "docs.tests.container": [
    "The fifth rung: the arrangement. The rung below proves the pose travels down the chain; this one is about the field that HANDS the poses OUT: row arithmetic in pixels, the reflow when a card arrives or leaves, and the other source of area under a desk with no box of its own.",
    "Each claim is measured on the glass against the etalon, not read off that same `placeChildren` the unit suite already trusts: 'the layout said put it in a row' and 'a row on the glass' have been different statements in this project before. The cards have different widths on purpose — a row of equal cells would fail the very first sum."
  ],
  "docs.tests.container.row": "Five claims about row arithmetic. Widths add exactly and nothing smears along the height; a gap is DAYLIGHT — between neighbours the desk shows through, and the span grew by exactly the gap; a lone card stands in the middle, because there are N−1 gaps and the edges owe no air; a free layout after a row is the same picture pixel for pixel as before it — the row does not write its answer to where a free one reads; and a doubled card gets doubled room — the row reserves the scaled footprint, not the declared box.",
  "docs.tests.container.flow": "Four claims about the reflow. An arriving card rebuilds the WHOLE row — the span grows by width and gap and stays centered, which is only possible if everything moved; a departing one closes the seam by exactly a width and ONE gap; a swap of insertions mirrors the picture — place follows tree order, not id or size, and cards are told apart by height, which shows in the probe; and an unknown layout name places nobody and does not carry off the scene — the card sits where its own pose put it.",
  "docs.tests.container.spread": "Four claims about the other area. A desk with no box carries a tight wrap of what it holds — not a pixel more; the area is a SIZE, not an address: `contentExtent` answers \"how much\" while the paint lands on the node's own middle — a card that walks away leaves the middle of the desk to the desk's colour, and the desk grows without moving house; padding is air around the wrap, it is there even in a record that places nobody, and its rim is painted in the DESK's colour, not the card's and not the bare glass; and children with no boxes occupy nothing — the desk has nothing to lie on and the glass is bare — zero pixels, not merely fewer.",
  "docs.tests.surfaced.openwork": "Five claims about a shape with no inside. One place is not a contour and paints nothing at any width; a solid line is walked out and back, so it has no free end and a round cap changes the picture not at all; cut it into dashes and the cap matters at once, because every dash has two ends of its own; an arrow is one path whose ends are set apart — a second head is more ink, the near end costs exactly what the far one does, and neither reaches past the run that was asked for; and the same walk under the stock `ink` and `trail` comes out solid or dashed, because the pattern belongs to the record.",
  "docs.tests.tiltable": [
    "The tap, on the glass. Tiltable answers one angle and nothing more, so the proof it is wired is that the answer moves the card: what the unit suite states about tiltAngle and nextTilt in the abstract, this rung makes the renderer show.",
    "The card is a 1×1.4 quad — taller than wide. A quarter turn swaps those two sides, a change no other field makes, so the claims are read off the ink's bounding box: upright it is portrait, tapped it is landscape, and the swap is exact."
  ],
  "docs.tests.tiltable.tap": "Three claims on one card. A stop turns it — upright the box is taller than wide, tapped to 90° it is wider than tall, and the two sides swap exactly, a real quarter turn about the centre with no smear at the right angle; three stops a third of a circle apart each leave a different picture, so the runtime index is genuinely read; and an index past the end clamps onto the last stop — the same picture, not a blank card and not a wrap, because wrap moves a tap one past the end and does not rescue an index already out of range.",
  "docs.tests.coated": "Coated on the glass. The unit suite states what the effect returns; this rung proves it is WIRED — a coat changes the picture, the level is a continuum, and a cast reaches a child.",
  "docs.tests.coated.play": "Three claims on one tile. A wash changes the picture (a coat is over the surface, not beside it); two levels of one recipe are two pictures (the magnitude is the parameter, no scene per value); and a cast greys the tray AND the child on it — the reach is the inheritance class, with no code walking the tree.",
  "docs.tests.flippable": "Flippable on the glass. The unit suite states the parity; this rung proves the engine's flip effect is WIRED — a turn shows the back, an even count returns the front, and a stack turns the card standing on it.",
  "docs.tests.flippable.play": "Three claims. An odd turn count swaps the surface to the back; an even count is the authored front again (the side is a parity, not a stored flag); and a stack turned once turns the card it holds — turns sum along the chain, so the child's summed parity is odd with no per-card bookkeeping.",
  "docs.tests.draggable": [
    "Draggable on the glass. The atom is a marker and one word; the proof it is WIRED is a pointer actually moving ink. So this rung plays synthetic pointer events against the same canvas a reader drags — the same pick, the same wiring, the same clock — and reads every claim back as pixels.",
    "The carry runs with no pop and no lean on purpose: the feel knobs are the shelf's lesson, and this rung measures POSITIONS — a pure translation returns a bit-identical picture, and bit-identical is the strongest claim a canvas can make."
  ],
  "docs.tests.draggable.drag": "Four claims on one desk. A grabbed card's paint leaves its seat and arrives under the pointer — patiently, because the spring TRAILS the finger and the lag is the feel, not a bug; a drop nobody accepts under onReject home flies the card back until the canvas is bit-identical to the start, because the tree was never written; the same drop under stay strands the card at the release seat, now its rest, and the old seat is bare desk; and the stone — no atom — refuses the finger pixel for pixel: no grab, no flight, no change.",
  "docs.tests.shadowCaster": [
    "The shadow on the glass. The plan states where a shadow falls; this rung proves ink actually lies there — darkness beyond the caster's own edge, on the lamp's side and nowhere else.",
    "Every probe sits just OUTSIDE the card's contour, so what it reads is never the face: it is desk, or desk under shadow. The two sides of the card are the whole story — the lamp's side is dark, the far side is bare."
  ],
  "docs.tests.shadowCaster.cast": "Three claims on one card. The fall is inked — just outside the down-left edge the desk lies in shadow while the up-right rim is bare, because the stock lamp hangs top-right; turn the lamp to the opposite corner and the same two probes answer the other way around — one ROOT field moved every fall; and height stretches the reach — a probe half a card off the edge is bare at rest and shaded at z 4, same lamp, same card, only the height moved.",
  "docs.tests.inviting": [
    "The invite on the glass. The unit suite states who is willing and what goes on; this rung proves the paint answers the FINGER: the same synthetic pointer the drag rung uses picks a card up, and one pixel in the zone's middle tells the whole story.",
    "The invite is a wash, so the middle changes colour while the right card is held — before, lit, and after are three readings of one point."
  ],
  "docs.tests.inviting.invite": "Three claims on one zone. Pick up the seven and the wash goes on — the zone's middle changes while the card is held; let go and the coat comes off EXACTLY — the middle reads the very value it did before the grab, because the undo restores what stood there rather than painting \"something else\"; and pick up the eight and nothing ever lights — the rule answered deny, and an unwilling zone has nothing to show.",
  "docs.tests.grippable": [
    "The grip on the glass. The unit suite states who may lift; this rung proves the FINGER obeys — the same synthetic pointer as the drag rung, judged from the seat north.",
    "Every drag ends where it began, so a refusal has the strongest possible reading: the whole canvas is bit-identical, no grab, no flight, no invite, nothing."
  ],
  "docs.tests.grippable.hands": "Three claims from one seat. North's finger moves north's card — out to the middle, back, and home; south's card ignores the very same finger pixel for pixel, because the grip sits on the HAND and cuts the subtree — the refusing card never carried the atom; and the open card, under no grip at all, travels for anyone: the open desk is the default, not a granted right.",
  "docs.tests.private": [
    "Privacy on the glass. The unit suite states who may see; this rung proves the PROJECTION obeys — a seat's view is a tree with the denied subtrees simply gone, and three pixels tell the whole story: my hand's spot, the other's, and the public pile's.",
    "The strongest claim is the last: swap the seat away and back, and the canvas is bit-identical — the view is local, and nothing was ever written to the truth."
  ],
  "docs.tests.private.hands": "Three claims through two pairs of eyes. North's view paints north's card, leaves south's spot bare desk, and shows the public pile; project the same truth for south and the two hands trade places while the pile stays; and reproject for north again — the canvas returns bit for bit, because a projection is pure: the hands were never written, only shown and not shown."
}
