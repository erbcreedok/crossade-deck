{
  "docs.presetsShuffles.component": [
    "The recipes a reorder can LOOK like — one shelf, in isolation, drop-in. A shuffle is three layers a game keeps apart: the ORDER (a seed through the kit's rng, or the server's word), the TRUTH (`reorder` — the same children in a new order), and the LOOK — a recipe here. A recipe is a pure function of `(index, count, progress, rest pose, group extent) → in-flight pose`, played by the one clock; at progress 1 it returns the rest exactly, so a shuffle ends with no jerk, and at its commit phase its pose does not depend on the seat, so the reorder underneath moves nothing on the glass.",
    "",
    "TAP ANY TOKEN and the row shuffles again; `shuffled` on the panel does the same. Shown on a row of plain tokens, not cards, on purpose: a recipe is entity-agnostic — children by index, cards or tiles or dice. A fan of a hand is the cards preset's business.",
    "",
    "```ts",
    "import { installStockShuffles, permutation, reorder, seededRng } from \"game-kit\"",
    "installStockShuffles()                       // riffle · overhand · wash · shake",
    "const order = permutation(hand.children.length, seededRng(seed))   // the truth, seeded",
    "motions.shuffle(hand.id, () => reorder(hand, order), { recipe: \"riffle\" })",
    "```",
    "",
    "A game registers its own with `registerShuffle(name, { poseAt, commitAt })`; an unknown name plays `riffle` rather than throwing."
  ],
  "docs.presetsShuffles.riffle": [
    "The pack splits into two halves that part to either side FANNED — no piece hiding another — and zip back together onto the new seats, the halves alternating in time so the merge reads as teeth meshing. The half a piece belongs to is its ORIGINAL half; where it lands is wherever the new order seats it. Nothing is ever swapped: a node keeps its face from the first frame to the last, and with no pile there is no top piece for the reorder to change in plain view. `riffle({ reach })`: `group` (the default) keeps it all in the picture, watchable end to end; `glass` carries the halves off the edge, for a pack too deep to fan."
  ],
  "docs.presetsShuffles.overhand": [
    "The pack in three packets: each is pulled STRAIGHT UP off its own places in turn (a layer higher than the last), carried across to the drop, spreading into a fan there, and let fall onto the new seats — the shape of a hand taking packets off the top and dropping them back. The carry is not decoration: a packet parked at the top is STILL at the frame the tree turns over, and the paint order turns over with it, so two packets lying on each other visibly trade places — precisely the swap of faces a shuffle must never show. Here every packet is mid-carry at that frame, and its pose is built from two fixed points, so it owes the seats nothing exactly when the seats move. `overhand({ reach })`: `group` (the default) keeps the packets in the picture; `glass` lifts them clean off the top."
  ],
  "docs.presetsShuffles.wash": [
    "Every piece scatters to a ring around the group, turning as it goes, and the ring gathers back onto the new seats — the tiles-on-a-table shuffle, for pieces that have no order to riffle. Each piece keeps its own spoke, so nothing hides anything. The radius is `wash({ radius })` in units; `0` (the default) works out a ring that just clears the group."
  ],
  "docs.presetsShuffles.shake": [
    "The group trembles — and all the while it trembles, the pieces keep landing on each OTHER'S seats: dice in a cup, tiles in a bag, the previous client's scramble. The permutation of a beat is drawn from the beat number, so it is a picture rather than a truth and it is the same on every frame and every machine; the tree's own reorder happens under the trembling, and the last quarter carries every piece home to the order it will keep. `shake({ strength })`: harder is a wider tremble and oftener changes of hand; `0` only trembles."
  ]
}
