## UNIT · motion — the settle clock

`vitest + a fake clock` · 23 кейсов, расписано 23

Плавность вместо телепорта: узел держит идентичность через смену дерева, и пружина играет его к новой
позе покоя (НЕ diff двух деревьев — та машина похоронена, `container.no-state-diffs`). Чистая
арифметика — `core/motion.ts` (интерполяция позы, реестр easing, `sample`); рантайм — `render/animator.ts`
(единые часы, `hold`/`release` для жеста, idle-гейт); шов рендера — `overrides` в `scenePlan`. Часы
инъектятся, поэтому «карта ровно посередине» проверяется тестом, а не удачей снимка. Дизайн —
`docs/design/transaction.md` (поза покоя derive/stamp/keep, «одни часы на всё»).

| id | Дано | Когда | Тогда |
|---|---|---|---|
| `motion.lerps-to-its-target` | `Motion` from→to за span | `sample` в началe/середине/конце и за краями | старт=from (не done), середина покомпонентно между, конец=to и done; поздний и ранний кадр клампятся, не перелетают |
| `motion.eases-and-clamps` | сток-easing установлен | `easeOut` в 0/0.5/1; settle нулевой длины | 0→0, 1→1, в середине уже за 0.5 (быстро ушёл); нулевой span — done с первого чтения, сразу в `to` |
| `motion.override-relocates-a-quad` | узел с поверхностью, `overrides` даёт позу в юнит-пространстве | `scenePlan` с оверрайдом vs без | квад встаёт по оверрайду (unit→px), покоя не касается; остальной конвейер о полёте не знает |
| `motion.settles-to-the-new-pose` | аниматор на фейковых часах, узел сдвинут | `setRoot`, затем `tick` в середину и конец | кадр ноль — в `from` (не телепорт), середина между, конец у цели; по прибытии кадр не планируется (idle-гейт) |
| `motion.a-held-node-tracks-its-gesture` | узел под `hold` | два `setRoot` подряд, затем `release`+`setRoot` | пока держат — прыгает в позу дерева 1:1 без пружины и без цикла; после `release` следующий ход снова едет (кадр запланирован) |
| `motion.a-new-node-appears-without-flying` | аниматор идёт, добавлен новый узел | `setRoot` | новый узел сразу в своей позе покоя, ниоткуда не летит: цикл не запущен |
| `motion.flip-squeezes-to-an-edge-at-the-midpoint` | `flipScale(t)` — проекция поворота | чтение в 0/0.5/1 и по краям | полная ширина по краям, ноль в середине (ребром), симметрично; клампится, не шире полной |
| `motion.flip-squeezes-to-an-edge` | аниматор на фейковых часах, `flip(id, commit)` | `tick` в 0/середину/конец | ширина (`transform.a`) полная→ноль(ребро)→полная; по прибытии кадр не планируется |
| `motion.flip-commits-at-the-edge` | `flip` со счётчиком в `commit` | `tick` до/на/после середины | сторона меняется РОВНО раз, на середине (ребром), не раньше — старая грань пока карта широкая |
| `motion.flip-rests-on-the-new-side` | переворот доигран | три `tick` до конца | карта лежит новой стороной, полная ширина; смена позы покоя от `commit` не запустила второй полёт |
| `motion.grab-places-the-run-under-the-finger` | аниматор, `grab(items, {anchor})` без подъёма и крена | захват | узел рисуется под пальцем сразу на полную дельту (1:1, без easing), кадр не планируется |
| `motion.grab-then-drag-rides-the-finger` | `grab`, затем `dragTo` шагами вбок | чтение позы на КАЖДОМ кадре, затем до оседания | ран стоит РОВНО под пальцем каждый кадр (1:1, нулевое отставание — лаг позиции читается вялостью, а не весом); пружина погони докручивается рядом, крен распрямляется, цикл засыпает (idle-гейт) |
| `motion.a-shadow-rides-the-hand-and-waits-out-a-flight` | стол с кастером, `grab`+`dragTo`, затем `release`+`setRoot` недвинутого дерева | тень в захвате, в движении пальца и в середине полёта домой | в руке падение УДЛИНЕНО и тень едет с картой один-в-один; в полёте карта далеко над столом, а тень уже стоит в точке реста, куда та прилетит — высоту даёт рука, не клок |
| `motion.grab-leans-into-horizontal-motion` | `grab` с `tilt`, `dragTo` вбок | `tick` в движении и до покоя | пока едет — поза несёт поворот (крен, `b≠0`), в покое крен распрямляется в ноль |
| `motion.a-reversed-drag-swings-the-lean-across` | `grab` на дефолтной настройке, палец едет 45 кадров вправо и разворачивается на 45 влево | угол НАРИСОВАННОЙ позы на каждом кадре | крен всё так же доезжает до упора (закон не «убрать наклон»), но перевал через ноль идёт СВОЕЙ пружиной: ни один кадр не двигает угол больше чем на 5°, и между двумя упорами карта проводит не меньше 8 кадров. Взятый прямо со скорости крен насыщён, поэтому разворот менял бы `+leanMaxDeg` на `-leanMaxDeg` за четыре кадра — 34° за 67 мс, щелчок |
| `motion.a-carried-node-settles-from-the-finger` | несомый `grab`+`dragTo`, затем `release`+`setRoot` того же (недвинутого) дерева | оседание домой | кадр ноль у пальца (не телепорт), затем едет и приходит в исходный покой — дерево не писалось ни разу |
| `motion.a-flying-node-rides-above` | стол: карта z=0 и стена z=5; карта отправлена в полёт `setRoot`'ом | `tick` в кадре ноль, середине, конце | в полёте карта рисуется ПОСЛЕ стены (поверх, с первого кадра), её `z` не переписан; по прибытии подъём кончается вместе с полётом — порядок по высоте возвращается, цикл спит |
| `motion.tune-patches-the-defaults` | `DEFAULT_TUNING`; патч `{settleMs: 240, lift: undefined}` | `tune()`; `tune(patch)` | без патча — сам рекорд; в патче меняется только названное, `undefined` не стирает дефолт; каждое поле — число или имя реестра (то, что умеет держать контрол) |
| `motion.tuning-defaults-apply` | `attachMotion` БЕЗ опций; `grab` БЕЗ опций | сдвиг узла; захват | settle идёт `DEFAULT_TUNING.settleMs` (в половине — между, в конце — дома); голый захват ПОДНИМАЕТ на `DEFAULT_TUNING.lift` (кадр планируется, масштаб приходит к 1.06) — дефолт есть то, чем все пользуются |
| `motion.settle-obeys-viewer-speed` | `settleMs: 100`; `viewer.motionSpeed = 2`, затем `0` | сдвиг узла, `tick` | при 2 — прилёт за половину задуманного времени (idle на 50 мс); при 0 — сдвиг на стекле следующим кадром, целиком, без полёта |
| `motion.speed-changes-mid-flight-are-smooth` | settle на середине | `motionSpeed 1 → 0.5`, `tick` +16 мс | прогресс сохранён (не прыгает ни вперёд, ни назад), продвинулся лишь на 8 искривлённых мс; полёт продолжается |
| `motion.flip-has-its-own-clock` | `settleMs: 1000`, `flipMs: 100` | `flip`, `tick` 50 | коммит на середине `flipMs`, а не `settleMs`; к 100 мс цикл спит — у поворота свои часы в тюнинге |
| `motion.retune-changes-the-next-flight` | `settleMs 100`; полёт начат; `retune({settleMs: 1000})` | `tuning()`; `tick` до конца первого; второй сдвиг | `tuning()` отвечает новыми числами; НАЧАТЫЙ полёт доигрывает по своим 100 мс; следующий идёт 1000 мс — экран настроек дизайнера перетюнивает живой стол без пересборки |
