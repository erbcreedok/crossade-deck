## INTERACTION · play functions

`@storybook/test + userEvent, Vitest browser mode` · 86 кейсов, расписано 81

| id | Дано | Когда | Тогда |
|---|---|---|---|
| `play.drag.commit` ⏳ | a Draggable element on a board | userEvent drags it to a slot and releases | it lands in the pointed slot; `dragStarted`/`dropAccepted` fired in order |
| `play.drag.reject-home` ⏳ | a drop the zone rule denies | released over the zone | flies home; ends at origin; `dropRejected` fired |
| `play.flip.by-echo` ⏳ | a face-down card | flip requested | stays old-side until the server echo, then flips (never blinks the face) |
| `play.keyboard.actionable` ⏳ | a Button (Actionable+Focusable) | Tab then Enter/Space | focuses, then fires the command — operable without a pointer |
| `play.toggle-atom.live` ⏳ | the Element scene | Bounded toggled off in Controls | not just the shadow: Surfaced, Draggable and ShadowCaster VANISH with their fields, and the scene is no longer an Element |
| `play.state-plane` ⏳ | the State tab | drag / flying picked | they apply, and they are absent from the spec payload — you cannot author them |
| `play.viewer-plane` ⏳ | viewer switched owner → other on a Private node | the canvas | the node is gone from the picture; the spec is untouched and nothing was sent |
| `e2e.checks-run-only-when-asked` | стори каталога и обе стори раздела Tests | открыты все | у каталожной нет ни одного шага; у Tests/Node и Tests/Bounded каждый шаг виден по имени и без падений. Выключатель — САМ РАЗДЕЛ: открыть страницу и есть просьба, а Storybook играет `play` на каждом рендере, так что вне раздела проверок нет вовсе |
| `play.node.it-is-there` | карта в дереве | пиксель в середине холста | не цвет стола. Единственное измерение, которого не сделает ни один слой выше: jsdom не знает WebGL, и «план велит нарисовать квадрат» — не то же, что «квадрат на стекле» |
| `play.node.it-goes` | то же дерево без карты | скормлено сцене | середина читается как голый стол, детей ноль |
| `play.node.it-comes-back` | дерево снова с картой | скормлено | карта на месте, и холст ТОТ ЖЕ. Новое дерево — новые данные: пересборка витрины потратила бы второй контекст WebGL |
| `play.node.repeats` | двадцать кругов туда-обратно | пиксель сверен с первым | не сдвинулся. Рендерер, который НАКАПЛИВАЕТ — спрайт добавлен вместо замены, слой отрисован дважды — верен на первом кадре и неверен на двадцатом |
| `play.node.a-scene-grown-while-starting-still-paints` | своя сцена в коробке 2px, выращенной тем же вздохом — resize приходит в окно старта рендерера | стекло прочитано после `ready` | карта на стекле В ПОЛНЫЙ размер: и чернила есть, и их габарит — карта, а не игла. До фикса ровно этот холст приезжал пустым на телефоне после перезагрузки |
| `play.debug-layer` ⏳ | Bounded on, Surfaced off, hit layer enabled | the canvas | an invisible node becomes inspectable — the only way to see a box |
| `play.bounded.the-outline-is-the-only-ink` | Bounded без Surfaced, отладочный контур включён | весь буфер прочитан | чернила есть, а середина коробки читается как стол: у Bounded нет заливки, и закрашенный квадрат означал бы, что отладочный слой врёт про атом |
| `play.bounded.size-is-on-the-glass` | `rect{1,1}` заменён на `rect{2,1}` | габарит чернил сверен | ширина выросла примерно вдвое, высота — нет: коробка, растущая по обеим осям от одноосной правки, это масштаб, а не размер |
| `play.bounded.every-shape-draws-its-own-pattern` | квадрат → круг → звезда → путь на одном холсте | снимки соседей сверены | каждый узор отличается от предыдущего. Слой, боксующий всё подряд, прошёл бы проверку размера и упал ровно здесь: круг и его габаритный квадрат различаются только рисунком штриха |
| `play.bounded.no-box-no-ink` | узел без `Bounded` | чернила посчитаны | ноль, а не «меньше»: обводить нечего, и любой уцелевший пиксель — отметка, которую слой забыл забрать |
| `play.surfaced.the-fill-is-the-box` | `rect{1.6,1}` с заливкой без радиуса и обводки | габарит чернил сверен с арифметикой (`unit × dpr`) | ровно объявленная коробка по обеим осям. Сверка с ЧИСЛОМ, а не с записанным эталоном: заливка, вышедшая квадратной, прошла бы проверку «чернила есть» и падает здесь |
| `play.surfaced.nothing-to-paint-with-or-on` | коробка без `Surfaced`; затем `Surfaced` без площади | чернила посчитаны | ноль в обоих случаях. Отладочный слой выключен, поэтому это утверждение о САМОЙ поверхности; голодающий атом — законное состояние, а не ошибка и не падение |
| `play.surfaced.the-colour-is-the-record-s` | два прогона, узел не тронут | запись перерегистрирована другим цветом | середина сменила цвет, габарит чернил не сдвинулся. Поля на узле этого не показали бы: пришлось бы обойти все узлы, и «коробка осталась» ничего бы не доказало |
| `play.surfaced.the-border-is-its-own-ink` | запись без слоёв, одна обводка внутрь | прочитан весь буфер | чернила есть, середина читается как стол, габарит совпадает с заливкой той же коробки; смена цвета обводки меняет картинку и не меняет геометрию |
| `play.surfaced.width-and-dashes-are-absolute` | тот же ободок шириной 0.04 → 0.12 → пунктиром | чернила посчитаны | втрое шире — втрое больше чернил при том же габарите; пунктир того же ободка даёт заметно меньше. Картинка бордера, растянутая по площади, так не ответит |
| `play.surfaced.a-different-record-is-a-different-picture` | `plate` → `bare` → `zone` на одном холсте | снимки соседей сверены | каждая картинка отличается от предыдущей: имя читается, а не игнорируется |
| `play.surfaced.the-paint-need-not-match-the-box` | обводка `alignment: 0`; затем слой-картинка `fit: contain` другой пропорции | габарит чернил сверен с габаритом заливки | наружу — шире коробки на ширину обводки с каждой стороны; картинка — уже коробки по одной оси. Расхождение surface и bounds видно на стекле, а не выводится из плана |
| `play.surfaced.a-desk-takes-its-area-from-what-it-holds` | `Container + Surfaced` без `Bounded`, двое детей с коробками | ребёнок убран | площадь равна раскладке (двое по 0.6 плюс зазор 0.12), то есть ШИРЕ любой коробки в дереве, и сжимается вместе с содержимым: `Surfaced` требует площадь, а не коробку |
| `play.surfaced.an-empty-desk-has-no-area` | `Container + Surfaced` без `Bounded`, детей ноль | чернила посчитаны, затем добавлен один ребёнок | ноль, а потом снова есть. Выводить площадь не из чего — это голодающий атом на столе и то состояние, в котором зона живёт до первой карты; и это не защёлка |
| `play.surfaced.content-without-boxes-is-no-area` | тот же контейнер с тремя детьми БЕЗ `Bounded` | чернила посчитаны | ноль: площадь дают КОРОБКИ детей, а не дети. Без этой строки предыдущая прошла бы и у рендерера, который считает наследников |
| `play.surfaced.a-box-of-its-own-wins` | узел с `Bounded{0.5×0.5}`, `Container` и двумя детьми по 0.6 | габарит чернил сверен с арифметикой | 0.5 по обеим осям, а не 1.32 ряда: выведенная площадь — ЗАПАСНОЙ ответ, а не сумма. Два ответа далеко разведены, поэтому измерение называет сработавшее правило |
| `play.surfaced.the-layout-decides-the-area` | те же две коробки в раскладках `row` и `free` | габариты чернил сверены | 1.32 против 0.6 по ширине при неизменной высоте: площадь — не свойство детей, её решила раскладка, а `free` не ставит никого |
| `play.surfaced.one-place-is-not-a-contour` | `polyline` из одной точки, толстая обводка и заливка | чернила посчитаны, затем добавлена вторая точка | ноль, потом есть. Одно место ничего не протягивает и не замыкает: ни контура обвести, ни площади залить. Форма законна, рисовать в ней нечего |
| `play.surfaced.the-contour-closes-itself` | линия из двух точек, сплошная обводка, `cap` butt → round | снимки сверены попиксельно | картинка ТА ЖЕ. Форма — область, путь замыкает себя сам, линия проходится туда и обратно, свободного конца нет, и `cap` нечего оформлять |
| `play.surfaced.a-dash-has-ends-of-its-own` | та же линия под пунктиром, `cap` butt → round | снимки и чернила сверены | картинка изменилась, чернил стало больше. Штрих — открытая ломаная при любом контуре, у каждого свои два конца, и круглый `cap` тратит чернила за ними |
| `play.surfaced.an-arrow-is-a-run-and-two-nodes` | `arrow` без наконечников, с одним, с двумя, и с одним на ближнем конце | чернила сверены, габарит сверен с длиной пробега | каждый следующий наконечник — больше чернил; ближний конец стоит ровно столько же, сколько дальний (одно описание, применённое дважды); ширина равна заказанной. Стрелки в модели нет: пробег — узел, каждый конец — свой узел |
| `play.surfaced.a-head-wears-its-own-record` | та же стрелка под `ink` и под `ink` + `endSurface: mark` | чернила посчитаны, ширина сверена | сплошной наконечник красит больше контурного, а пробег под ним не изменился: одна нода — одна запись, и заливка на волосяной линии возможна только так |
| `play.surfaced.the-pattern-lives-in-the-record` | один и тот же пробег под штатными `ink` и `trail` | чернила посчитаны, ширина сверена | сплошной красит больше пунктирного, а ширина у обоих одна: узор — свойство ЗАПИСИ, геометрия о нём не знает |
| `play.transformable.at-moves-the-picture-not-its-size` | карта без позы, затем `at{0.5,0.3}` | центр и габарит чернил сверены с арифметикой | без позы центр — середина холста; сдвиг — ровно столько пикселей по каждой оси, сколько юнитов сказано, а габарит не изменился ни на волос: положение — не размер |
| `play.transformable.the-pose-orders-itself` | `at{0.6,0}` и `scale: 2` в одной позе | центр и ширина чернил сверены | вдвое шире, а середина на 0.6 юнита, не на 1.2: масштаб-потом-перенос. Два ответа разведены на 0.6 юнита, и чтение называет порядок, а не просто согласуется с ним |
| `play.transformable.a-right-angle-is-exact` | коробка 1.6×1 под 90° и 180° | габарит чернил сверен | на 90° ширина и высота меняются местами с той же точностью, что у неповёрнутой; на 180° коробка — снова коробка. Плавающая грязь в прямых углах размазывает габарит — на симметричном квадрате её не видно |
| `play.transformable.a-full-lap-changes-nothing` | 30° против 390°, −90° против 270°, между парами — заведомо другой кадр | кадры сверены попиксельно | одинаковы: полный оборот — тот же угол, отрицательный — тот же круг в другую сторону. Другой кадр между чтениями — чтобы «одинаково» было измерением, а не незамеченной пропущенной перерисовкой |
| `play.transformable.scale-is-uniform-and-zero-erases` | `scale: 2`, затем `0`, затем `1` | габарит и чернила посчитаны | вдвое по ОБЕИМ осям; в ноль — ноль чернил без падения; назад в единицу — карта на месте в свой размер: не защёлка |
| `play.transformable.angle-adds-up-on-the-glass` | рука 30° с картой 15°, одиночные 30° и 45° | кадры сверены попиксельно | цепочка равна одиночным 45° и отличается от 30°: две программы — одна картинка, и сумма читается со стекла, а не с арифметики |
| `play.transformable.a-card-turns-about-its-hand` | рука `at{0.5,0}` под 90°, карта `at{0.4,0}` в ней | центр чернил сверен | карта ПОД рукой: смещение уехало в экранное `y` вниз (это и есть «по часовой»), ось — начало владельца. Вокруг середины стола карта улетела бы на юнит — класс бага, который выглядит как физика |
| `play.transformable.scale-multiplies-on-the-glass` | рука 0.5 с картой 0.5, затем с молчаливой картой | ширина чернил сверена | четверть объявленной ширины, у молчаливой — половина: сумма сказала бы единицу, ближайший — половину, и одно чтение различает все три |
| `play.transformable.a-layout-writes-at-on-the-glass` | те же две коробки с теми же позами под `free` и `row` | размах чернил сверен | 2.4 юнита против 1.32 ряда: свободная раскладка не ставит никого и позы стоят, ряд пишет `at` обоим — и набранные позы перестают значить |
| `play.transformable.a-reparented-card-answers-anew` | ОДИН объект карты из полуразмерной руки в полную, дерево то же | ширина чернил сверена на следующем кадре | половина, потом полный размер: цепочка читается в момент применения. Замороженная база — тот самый способ, каким веер ломался трижды |
| `play.transformable.z-decides-the-cover-and-moves-no-box` | две перекрывающиеся плашки разных цветов, первой дан `z: 1` | пиксель перекрытия и габарит сверены | перекрытие сменило цвет, объединение чернил не сдвинулось: подъём — не расстояние, и `z`, подрисовавший сдвиг, упал бы на второй половине |
| `play.transformable.z-adds-up-through-the-stack` | одиночная `z: 1` против карты БЕЗ высоты в стопке `z: 2`, затем стопка опущена | пиксель перекрытия сверен | накрывает карта стопки — её высота есть СУММА по цепочке; опускание стопки в одиночку возвращает покрытие, карта под ней не менялась |
| `play.transformable.equal-z-keeps-tree-order` | обе плашки `z: 1`, добавления переставлены, затем то же дерево скормлено повторно | пиксель перекрытия и кадры сверены | накрывает поздний сосед; перестановка добавлений меняет покрытие; повторная отрисовка не меняет НИЧЕГО — нестабильная ничья это мерцание карт между кадрами |
| `play.container.a-row-is-the-sum-of-its-parts` | штатный ряд из карт 1 и 0.5 юнита | габарит чернил сверен с арифметикой | ширина — сумма плюс штатный зазор 0.12 между ними, высота — ровно карта, центр — середина холста: ряд из равных клеток или забывший свой зазор провалил бы первую же сумму |
| `play.container.the-gap-is-daylight` | та же пара под записью с зазором 0.5 | размах и пробы сверены | размах вырос ровно на зазор, И в шве читается стол: ряд, растянувший сами карты, прошёл бы первую половину |
| `play.container.a-lone-card-centres` | одна карта под той же записью с зазором | центр чернил сверен | по центру: зазоров N−1, и внешний зазор столкнул бы одиночку на полшва |
| `play.container.a-layout-swap-does-not-latch` | free → row → free, позы набраны, между чтениями заведомо другой кадр | первый и третий кадры сверены попиксельно | одинаковы: ряд не пишет свой ответ туда, откуда свободная раскладка его потом прочтёт |
| `play.container.reserved-room-on-the-glass` | широкая карта с `scale: 2` рядом с узкой | габарит чернил сверен | 2.5 юнита ширины плюс штатный зазор и удвоенная высота: место забронировано под отмасштабированный габарит, без наезда |
| `play.container.an-added-card-reflows-the-row` | двое в ряду с зазором, затем трое | размах и центр сверены | размах вырос на ширину с зазором и остался по центру — значит, подвинулся ВЕСЬ ряд |
| `play.container.a-removal-closes-the-aisle` | из тройки ушла средняя | размах сверен | сжался ровно на ширину плюс ОДИН зазор, выжившие снова смежны: несвежая мерка дала бы другое число |
| `play.container.order-is-the-tree-on-the-glass` | высокая и низкая карты, вставленные в оба порядка | пробы над серединой сверены | высокая краска зеркалит сторону вслед за порядком вставки — место следует за деревом, не за id и не за размером |
| `play.container.an-unknown-layout-keeps-the-scene` | `layout: "carousel"`, карта с собственной позой | чернила и центр сверены | сцена жива, карта там, куда положила ЕЁ поза: пропавшая запись не ставит никого и не уносит остальных |
| `play.container.the-desk-wears-its-content` | стол без коробки с одной картой | габарит чернил сверен | всё покрашенное — ровно обёртка карты: стол не добавил площади нигде |
| `play.container.the-area-is-a-size-not-an-address` | карта ушла на 0.8, затем вторая на другом краю | размах, центр и пробы сверены | стол вырос, но не переехал: `contentExtent` отвечает «сколько», а краска ложится по центру УЗЛА — в середине стола теперь цвет стола, карта его покинула; двое на краях растягивают обёртку на расстояние плюс ширину, снова симметрично |
| `play.container.padding-is-air-around-the-wrap` | свободная запись с `padding: 0.3` | габарит и пробы сверены | обёртка выросла на 0.6 по каждой оси, кайма покрашена цветом СТОЛА — не стола-фона и не карты; сама карта не сдвинулась |
| `play.container.bare-children-leave-nothing` | у стола только дети без коробок | чернила посчитаны | ноль пикселей, не «меньше»: занимать нечем, и поверхности не на что лечь |
| `play.tiltable.a-stop-turns-the-card` | карта 1×1.4, упор 0 затем упор 1 (90°) | габарит чернил сверен | прямо коробка выше, чем шире; в тапе шире, чем выше; и стороны меняются местами РОВНО — высота прямой равна ширине тапнутой, ширина прямой равна высоте тапнутой: настоящая четверть оборота вокруг центра, а не подрисованный сдвиг |
| `play.tiltable.every-stop-is-its-own-picture` | упоры `0, 120, 240`, индекс 0 → 1 → 2 | снимки соседей сверены | каждая картинка отличается от предыдущей: резолвер, игнорящий индекс, покрасил бы одно и то же трижды и упал бы здесь |
| `play.tiltable.the-tap-clamps` | упоры `0, 90`, индекс 1 затем 5 | снимки сверены, чернила посчитаны | картинка ТА ЖЕ, и чернила есть: индекс за концом клампится на последний упор, а не бланчит и не заворачивает — `wrap` двигает тап на один за конец, но не спасает уже вышедший за край индекс |
| `play.coated.a-wash-changes-the-picture` | тайл без слоя затем `wash` accent | снимки сверены | картинка меняется: слой ПОВЕРХ поверхности, не рядом |
| `play.coated.the-level-is-the-picture` | `wash` level 0.2 затем 0.9 | снимки сверены | две картинки: величина это параметр, ни сцены-на-значение |
| `play.coated.a-cast-greys-the-tree` | поднос+ребёнок, cast "" затем `wash` | снимки сверены | меняются оба: cast (fromOwner) доходит до ребёнка без обхода |
| `play.flippable.a-turn-shows-the-back` | карта turnOver turns 0 затем 1 | снимки сверены | нечет свопит поверхность на изнанку (accent≠panelBg) |
| `play.flippable.re-flip-returns-the-front` | карта turns 0 затем 2 | снимки сверены | ТА ЖЕ картинка: сторона это чётность, не хранимый флаг |
| `play.flippable.a-stack-turns-its-child` | ребёнок под стопкой turns 0 затем 1 | снимки сверены | ребёнок показывает изнанку: turns суммируются по цепи |
| `play.flippable.content-swap-shows-another-subtree` | доска с contentSwap-рецептом потребителя, turns 0 затем 1 | снимки сверены | картинка сменилась ЦЕЛЫМ поддеревом (железо+кристалл), лицо не просвечивает |
| `play.flippable.move-then-flip-mirrors-the-live-state` | пешка сдвинута в +x, стол флипнут vs пешка в −x без флипа | снимки сверены | картинки СОВПАДАЮТ: зеркало ложится на живую позу, ничего не хранится (кейс D) |
| `play.flippable.params-change-on-the-fly` | живая смена: back-запись перерегистрирована; рецепт mirror вместо turnOver | снимки сверены | каждое изменение перекрашивает СТОЯЩЕЕ дерево — ни сцены-на-значение, ни пересборки |
| `play.draggable.a-card-follows-the-finger` | карта с атомом на столе, синтетические pointer-события против холста | down на карте, move в сторону | краска уходит с места и приезжает под указатель — терпеливо: пружина ОТСТАЁТ от пальца, лаг это ощущение, не баг; жест закрыт отпусканием на месте |
| `play.draggable.a-refused-drop-flies-home` | `onReject: home`, дроп никто не принимает | down → move далеко → up там | карта улетает домой, у точки отпускания краски нет, и холст совпадает бит-в-бит со стартом: дерево не писалось ни разу |
| `play.draggable.stay-strands-the-card` | то же дерево с `onReject: stay` | тот же жест | карта осталась на месте отпускания — это её новый покой, старое место — голый стол; сброс дерева уносит её обратно |
| `play.draggable.only-a-draggable-lifts` | камень без атома | down на камне → move → up | ни хвата, ни полёта: холст бит-в-бит тот же — pick спрашивает `draggable`, и пальцу отказывает ровно то, что глаз видит без атома |
| `play.shadow.the-fall-is-inked` | карта-кастер, штатная лампа сверху-справа | пробы чуть снаружи нижне-левого и верхне-правого краёв | со стороны лампы за краем — тень (не фон), с дальней — голый стол: чернила лежат там, куда план сказал падать, и нигде больше |
| `play.shadow.the-lamp-walks` | та же карта, `Lit` угол 135 | те же две пробы | ответы поменялись местами: одно КОРНЕВОЕ поле повернуло каждое падение — пер-предметного света не существует |
| `play.shadow.height-stretches-the-fall` | z=0, затем z=4 | проба в полукарте от края | в покое гола, поднятой — затенена: та же лампа, та же карта, двигалась только высота — z источник, достающая тень следствие |
