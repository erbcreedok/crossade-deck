## UNIT · handover — передача рана в зону

`vitest + jsdom` · 1 кейсов, расписано 1

`handOver` из `game-kit/src/render/handover.ts`: ран, ведомый ХЭНДЛОМ, отдаётся зоне здесь, потому что
проводка умеет пере-родить только собственный дроп. Картинка приземления (`isDrawn` — грип, метка) не
принадлежит рану и остаётся на месте.

| id | Дано | Когда | Тогда |
|---|---|---|---|
| `handover.a-run-moves-into-the-zone-but-the-landing-picture-stays` | зона и три узла: две карты и картинка приземления (грип) | `handOver` со всеми тремя id | обе карты стали детьми зоны; картинка приземления осталась на прежнем месте — `handOver` пропускает всё, что `isDrawn` |
