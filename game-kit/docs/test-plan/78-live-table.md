## UNIT · liveTable — живой стол целиком

`vitest + jsdom + фейковые часы` · 27 кейсов, расписано 27

`liveTable` из `game-kit/src/render/liveTable.ts`: одна проводка живого стола — своя оболочка,
когда потребитель не даёт своей; бросок, сеть и снятие. Каталог и хаб зовут ЕЁ, а не собирают стол
заново.

| id | Дано | Когда | Тогда |
|---|---|---|---|
| `liveTable.stands-a-desk-with-no-catalog-round-it` | Пустой контейнер, заглушка рисовальщика, стол без оболочки | Поднимают `liveTable` без `stage` | Рисовальщика просят ровно один раз, дерево на экране то самое, есть камера и рантайм часов |
| `liveTable.a-flick-leaves-the-hand` | Стол с картой, отпускание через `throw` | Быстрый флик пальцем и отпускание | Посадка записана в точке отпускания, а после прогона часов карта уехала дальше неё |
| `liveTable.a-tree-from-the-net-leaves-the-view-alone` | Камера уведена, подключено зеркало | `setRoot(next, "net")`, затем `setRoot(next, "me")` | Дерево из сети на экране, камера не сдвинулась, в комнату эхо не ушло; своя правка — объявлена |
| `liveTable.seats-idle-return` | Опция `seats` с местом и `idleReturn`, камера уведена от места | `idle.step` до и после дедлайна; `idle.input()` сбрасывает отсчёт | До дедлайна вид стоит; после — доехал до места читателя; после `input()` отсчёт начат заново |
| `liveTable.a-plain-desks-drop-tells-the-mirror` | Стол без стэкинга, `letGo: "drop"`, `actor` и зеркало | Спокойный релиз карты (без маха) | Зеркало оповещено ПОСЛЕ отпускания, и в дереве на тот момент уже стоит метка `moved` — не только на середине жеста |
| `liveTable.a-tap-that-turns-a-card-is-a-changed-desk` | стол с `Flippable` картой, `flipping: true`, `onDeskChanged` | тап по карте, часы докручены до конца поворота | сам релиз говорит «изменился» с картой ещё лицом вверх; поворот говорит ЕЩЁ РАЗ, и в дереве на тот момент карта уже перевёрнута — картинка руки на стекле перерисовывается по флипу, а не по следующему движению |
| `liveTable.the-place-a-run-came-from-takes-it-back-on-a-hit-and-not-by-its-pull` | стол со стекингом, рука-зона 2×2 в x=3 с досягаемостью 2, в ней карта; `zones` — `zoneNear` с `from` | медленный перенос из руки в x=0.5 (в зоне притяжения, но не на руке); затем новым жестом в x=1.3; затем из руки в x=3.4 (на руку) | вынутая и отпущенная в притяжении карта НЕ возвращается — лежит на сукне (притяжение не забирает то, что только что отдало); следующим жестом то же притяжение берёт; вынутая и положенная НА руку в том же жесте — снова в руке. Баг хозяина: карту нельзя было вернуть в руку тем же заходом пальца — проводка отказывала месту-источнику целиком, не отличая притяжение от попадания |
| `liveTable.a-finger-on-the-glass-between-the-furniture-drives-the-view` | стол на своей сцене (`painter`, комната шире стекла), `standIn` на картинку; на hud-корне тень (просто нарисована), плашка с `Pressable`, картинка карты | свайп по тени; по плашке; по картинке | над тенью камера сдвинулась (нарисованное — стекло); над плашкой стоит (мебель); над картинкой стоит (это способ дотянуться до карты). Жалоба хозяина: палец чуть ниже стола ложился на тень и камера молчала, а тап в том же месте доставал карту под ней — один палец, два закона |
| `liveTable.the-picture-of-a-landing-is-the-landing` | стол с картой `Carry: holder`, камера повёрнута на 90°, отпускание через `drop` | Медленный перенос карты и отпускание | Карта ложится под углом 270° — и контур нарисован под тем же углом и в той же точке (зазор меньше 0.15 при клиренсе 0.45) |
| `liveTable.a-deck-is-carried-as-one-body` | Стол с колодой из трёх касающихся карт `Carry: holder` (проводка рисует ей ручку), камера повёрнута на 90°, отпускание через `drop` | Медленный перенос колоды ЗА РУЧКУ и отпускание | Ручка, контур приземления и карты стоят на одной линии по «верху» держащего (поперечный разброс меньше 0.01); карты ложатся под 270° в точке, где стоял контур (зазор меньше 0.15) |
| `liveTable.a-piece-held-against-the-rim-brings-the-desk-to-it` | стол 40×40 с колодой, комната такая же, `clock` — сердцебиение потребителя; медленный перенос карты к левой кромке (x=6) | палец замер у кромки; двадцать ударов часов; затем палец в середину | у кромки сердцебиение подключено и держится (`true` каждый удар); вид уехал (`camera.x` изменился) — влево, к тому, что там лежит; груз остался под пальцем (по стеклу в пределах 40px от него), а не на уехавшем сукне (`refollow`); в середине вид стоит: удары ничего не двигают; палец НАД мебелью стекла (узел на hud-корне у нижнего края) ничего у вида не просит — иначе наведение на якорь, куда кладут руку, угоняет стол из-под пальца в тот самый момент, когда целятся |
| `liveTable.a-press-on-a-control-of-the-desk-is-answered-and-told` | стол с двумя кнопками (`button`, `Valued does`), `presses` отвечает `true` только на `shut`, зеркало считает `changed` | тап (down+up на месте) по `shut`, затем по `nope` | `presses` получил оба значения; после `shut` комната узнала ровно один раз (через `settle`, как после дропа); после `nope` — ни разу. Кнопки стола проводятся один раз (`wireButtons`) через камеру стола |
| `liveTable.a-picture-on-the-glass-carries-the-piece-it-stands-for` | стол с картой; на hud-корне — её картинка, `standIn` отдаёт по ней узел карты | палец опускается на картинку и ведёт | в воздухе оказалась КАРТА со стола (`poses` знает её), и она под пальцем — расстояние между картинкой и картой это полэкрана, а не хват: сохранённое, карта таскалась бы в стороне от руки. Экран стоит НАД столом, палец встречает его первым, а картинка карты — способ дотянуться до карты |
| `liveTable.a-felt-sized-ring-is-still-a-control` | стол с кольцом без `Screened`, но с `Carry: { ride: "felt" }` | Медленный перенос кольца, затем флик им | Под кольцом нет узла-метки; после флика кольцо стоит там, где его отпустили, и метки касания не заработало |
| `liveTable.a-control-is-carried-bare` | Стол с кольцом (`Screened`) рядом с картой, отпускание через `throw` | Медленный перенос кольца, затем флик им | Под кольцом не нарисован ни один узел-метка; после флика кольцо стоит там, где его отпустили, и метки касания не заработало |
| `liveTable.a-control-earns-no-mark` | Тот же стол с кольцом, актёр `south` на сцене, отпускание через `drop` | Перенести кольцо и отпустить, затем тем же жестом перенести карту | На кольце нет атома `Marked`; на карте — `mark: "moved"` |
| `liveTable.a-flick-survives-a-mirror-that-rewrites-the-desk` | Стол с картой, зеркало, чей `hand` переписывает корень (как это делает стол с людьми) | Быстрый флик и отпускание | Карта всё равно уехала дальше точки отпускания: доклад дальнему экрану не съедает полёт |
| `liveTable.a-carried-control-is-told-still` | Стол с кольцом и картой, подключено зеркало | Носят кольцо, затем карту | В докладе зеркалу кольцо помечено `still`, карта — нет |
| `liveTable.a-disc-is-not-a-thing-a-finger-reaches` | Стол с диском (`Screened`, БЕЗ `Draggable`) в стороне от карты | Палец кладут на диск и уводят | Диск стоит где стоял, и вместе с пальцем не уехало ничего другого |
| `liveTable.a-flick-with-a-control-on-the-desk` | Тот же стол с кольцом, отпускание через `throw` | Флик КАРТОЙ | Карта улетела дальше точки отпускания: отказ кольцу не отнимает полёт у карты |
| `liveTable.a-thrown-card-lands-as-its-holder-held-it` | Стол с картой `Carry({orient:"holder"})`, камера повёрнута на 90°, отпускание через `throw` | Флик картой | Угол карты В ДЕРЕВЕ = поворот держащего (270°) сразу после отпускания и после приземления: бросок не сбрасывает поворот на север |
| `liveTable.a-throw-turns-nothing-that-did-not-ask` | Стол с картой БЕЗ `Carry`, свой угол 30°, отпускание через `throw` | Флик картой | Угол остался 30°: «не спрашивали» — это не ноль, и бросок не расплющивает угол, заданный игрой |
| `liveTable.stop-takes-everything-off` | Поднятый стол | `stop()` | Оболочка снята, а палец на стекле больше ничего не двигает |
| `liveTable.seats-home-span` | круглый стол с `seats` | открытие и возврат домой | зум = `spanZoom` (1.5 ширины), одно число на оба |
| `liveTable.seats-home-width` | комната 30, сукно 10, `homeSpan` 1.5 | открытие и возврат домой | зум меряется по СУКНУ: 1.5 стекла на сукно, а не на комнату вокруг него |
| `liveTable.seats-insets` | стекло 393×740, `homeSpan` 1.5, сукно 20, `insets.top` 70 | открытие и возврат домой | зум МЕНЬШЕ запрошенного, верх сукна ниже плашки (≥ 70), и глиссада садится на то же число
| `liveTable.a-new-glass-keeps-its-reader-seated` | сидящий дома читатель, стекло 700 → 800 | размер стекла сменился | читатель остался дома; ушедший панорамой остаётся там, куда ушёл |
