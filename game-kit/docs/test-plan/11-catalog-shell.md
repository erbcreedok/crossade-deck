## UNIT · оболочка каталога

`vitest + jsdom` · 102 кейсов, расписано 102

Сцена, инспектор-шина, сниппет и слова — это КАТАЛОГ, а не кит: он живёт в `.storybook/` и
документирует то, что уезжает в игру. Строки были в коде без строк здесь — долг, закрытый задним
числом.

| id | Дано | Когда | Тогда |
|---|---|---|---|
| `scene.a-camera-scene-draws-through-its-camera` | сцена с камерой, карта в начале координат | камера панорамирована | план построен ЕЁ матрицей: квад уехал ровно на пан. Без камеры стол остаётся на месте, и двигаются одни числа |
| `scene.a-re-render-retunes-the-camera` | стоящая сцена с зумом 3 | перерисовка с новым потолком 2 | та же камера, и потолок применён СРАЗУ: пересобранная бросала бы читателя на середину стола при каждом нажатии в панели, а камера не имеет права терять, куда смотрела |
| `scene.toolbar-drives-this-canvas` | сцена | эталон выбран в строке над видом | хост получил его: настройка канваса стоит на канвасе |
| `scene.toolbar-auto-releases` | выбран `auto` | вьюер прочитан | оверрайд УБРАН, а не выставлен числом, которое лишь похоже на отсутствие |
| `scene.two-canvases-differ` | две сцены на странице | эталоны выбраны разные | у каждой свой; ни одна не говорит за соседку |
| `scene.catalog-cannot-overrule` | эталон выбран руками | переключён язык каталога | эталон не сброшен: каталог не перебивает настройку канваса |
| `scene.paints-what-the-tree-holds` | узел с коробкой и поверхностью | сцена смонтирована | рендереру отдан план дерева; оболочка своего не придумывает |
| `scene.a-change-is-not-the-time-to-write-the-note` | сцена, подписка `onInspect`, десять `setRoot` подряд | отчётов сразу; отчётов через тик | 0 сразу; 1 после тика. На каждую публикацию дерева оболочка строила ВТОРОЙ план стола ради строки «ничего не нарисовано», отчёт инспектора и тулбар — на пути ввода, треть стоимости переноса. Картинка рисуется сразу, слова о ней — следующим тиком, один раз на всю пачку |
| `scene.note-earns-its-words` | пустая сцена и нарисованная | заметка прочитана | «ничего не нарисовано» стоит только под пустым планом — под квадратом она учила бы обратному |
| `scene.repaints-on-a-viewer-change` | эталон 34 → 60 | план перерисован | фигура выросла: эталон это размер |
| `scene.bounds-toggle` | коробка и ничего красящего | кнопка нажата и отжата | отметки появились и ушли; НАРИСОВАНО по-прежнему ничего — инструмент не поверхность |
| `checks.pace-defaults-to-all-at-once` | стори без выбранного темпа | спрошен темп | всё подряд, ни одной паузы: открыть страницу Tests и есть просьба прогнать её проверки, и перезагрузка страницы возвращает сюда же |
| `checks.step-mode-pauses-after-every-step` | темп «по шагам», реплей до шага 2 | спрошены паузы по индексам | до цели — спринт без пауз, с цели и дальше — пауза ПОСЛЕ каждого шага: на стекле стоит картина самого шага |
| `checks.back-replays-to-the-previous-step` | индексы 3, 1 и 0 | спрошена цель «назад» | предыдущий шаг; у нулевого цели нет — сцена живая, назад это честный реплей, а не откат |
| `scene.a-resize-mid-start-is-not-lost` | рендерер стартует асинхронно, resize приходит в окно старта и рендерер его отвергает | старт завершён | обёртка пере-утверждает ПОСЛЕДНИЙ размер и кадр. Иначе холст остаётся с размером, замеренным до раскладки, — на телефоне это один пиксель, показанный как пустая сцена, которую «чинит» любой тумблер |
| `scene.bounds-is-this-canvas-only` | две сцены | отладка включена у одной | вторая не изменилась |
| `scene.bounds-survives-a-catalog-change` | отладка включена | переключён язык | осталась включённой, и кнопка это показывает |
| `scene.select-is-capped-not-truncated` | тулбар | ширина селекта и подписи опций | контрол капнут с многоточием, подписи ЦЕЛЫЕ: список рисует платформа, и он по-прежнему по контенту |
| `scene.tap-turns-the-owner-of-the-turn` | сцена с `flipOnTap`: стол, плашка на нём и точка на плашке | тап по точке, затем мимо всех узлов | перевернулась ПЛАШКА — ближайший, кто переворачивается сам, — а не точка под пальцем; промах достаётся корню, иначе лицо, подставленное самим переворотом, назад не вернуть |
| `scene.tap-is-wired-once` | стори вызвана повторно с тем же id | один тап | один переворот: слушатель живёт со сценой, а не с рендером — иначе каждое движение ползунка добавляло бы ещё набор, и тап возвращал бы карту на место |
| `scene.a-tap-reports-what-it-landed-on` | сцена с `tap`, эталон 60 | тап по точке и по голому сукну; перерисовка с ДРУГИМ обработчиком; тап после сноса | оболочка отдаёт попадание (`dot`) и `undefined` за пределами дерева — дальше решает стори; после перерисовки отвечает НОВЫЙ обработчик и ровно один раз (слушатель по-прежнему один); снесённая сцена не слышит ничего |
| `scene.a-rerender-feeds-the-scene` | стори вызвана повторно с тем же id | построена сцена | вернулась ТА ЖЕ сцена с тем же `view`, в ней новое дерево: аргумент — это новые данные, а не новая сцена |
| `scene.settings-survive-new-data` | выбран эталон 34 и включены bounds | стори вызвана повторно | обе настройки на месте — зритель не менялся |
| `scene.a-tree-swap-redraws` | сцена с нарисованной плашкой | корень подменён на голый узел | план опустел: подмена объявляет себя, иначе новое дерево появилось бы на следующем постороннем событии |
| `scene.animate-hands-over-the-clock` | сцена с `animate` и сцена без | прочитан `motions`; сыгран `grab` | у неподвижной ручки нет; у анимированной есть, и она ЖИВАЯ — grab рисует узел у пальца тем же художником: второй рантайм был бы вторыми часами на одном стекле |
| `scene.toolbar-follows-language` | строка тулбара | переключён язык | подписи перечитаны, а не вшиты |
| `textSize.pins-the-root` | документ каталога | стиль установлен | раздувание шрифта выключено, с префиксом, который слушают телефоны. `100%`, а не `none`: последний на iOS отбирал ещё и щипковый зум |
| `textSize.pins-once` | стиль уже стоит | установлен повторно | одно правило, а не второе такое же: превью ставит его при загрузке модуля, менеджер — на своём старте |
| `textSize.both-documents` | исходники менеджера и превью | просканированы | закрепляют оба. Каталог — два документа без общей таблицы стилей, и в первый раз менеджер был пропущен: панель «Код» приходила правильной и удваивалась на первом же переключении вкладки, пока проза рядом стояла на месте |
| `inspector.bus-late-subscriber` | сцена уже построена | панель подписалась после | получила отчёт: сцена ПУБЛИКУЕТ, а рисует кто хочет |
| `inspector.bus-gone-scene` | сцена уничтожена | отчёты прочитаны | её больше нет: мёртвая сцена не говорит за дерево |
| `inspector.bus-dedupes` | сцена опубликовала один и тот же отчёт дважды | подписчики шины | вызваны ровно раз: одинаковый отчёт подряд не публикуется и не сериализуется |
| `inspector.bridge-watchers` | мост инспектора через канал Storybook | события watch / unwatch и публикации сцены | без смотрящих канал молчит; на watch сразу приходят все live-отчёты; на unwatch канал снова замолкает |
| `inspector.bridge-recreated` | смотрящий подписан | мост превью пересоздан | мост спрашивает «кто смотрит?» через GK_INSPECT_WHO; на ответ смотрящего отчёты идут снова |
| `inspector.markup-is-pure` | список узлов | разметка построена | без сцены и без документа — та же разметка идёт и в панель менеджера, и под канвас доков |
| `inspector.markup-empty` | ни одной сцены | разметка построена | пустое дерево, а не падение |
| `inspector.scene-named-after-story` | страница доков | сцены построены | каждая названа своей стори, и блок находит свой канвас |
| `inspector.two-scenes` | две стори на странице | обе опубликовали | не перезаписали друг друга |
| `inspector.open-persists` | блок свёрнут | открыта другая стори | свёрнутость запомнена |
| `inspector.tab-starts-on-the-tree` | карточка под сценой, выбора ещё не делали | открыта | показано дерево: в этом срезе это единственное, что у сцены есть показать |
| `inspector.tab-persists` | выбрана вкладка «controls» | открыта другая стори | выбор запомнен |
| `inspector.tab-forgets-a-name-that-is-gone` | в localStorage лежит вкладка, которой больше нет | прочитана | дерево, а не пустая карточка без объяснений |
| `source.is-the-story` | стори | сниппет построен | это ИСХОДНИК, а не отрендеренный DOM |
| `source.imports` | сниппет | прочитан | сказано, откуда имена, иначе читается как магия |
| `source.scene-becomes-mount` | сниппет со `scene(...)` | прочитан | переписан в `mount`: `scene` живёт только на этом сайте, читатель его не импортирует |
| `source.the-drag-wiring-unwraps` | сниппет с `wireDrag(scene(...), …)` | прочитан | обёртка развёрнута до своей сцены, `wireDrag` в сниппете нет, и одна строка-комментарий говорит, что тут стояла проводка пальца: имя каталога не преподаётся как китовое, но и сцена «сама тащится» не преподаётся |
| `source.imports-follow-the-rewrite` | тот же сниппет | прочитана строка импорта | импортируется `mount`, потому что показан `mount` |
| `source.a-story-becomes-a-program` | стори с `render` и `parameters` | сниппет | обёртки нет: ни `render:`, ни ключей доков, тело с нулевой колонки |
| `source.args-become-constants` | стори с `args` | сниппет | каждая ручка — `const`, на который тело по-прежнему ссылается |
| `source.a-comma-in-a-name-is-text` | арг со значением `"hand, discard"` | сниппет | целый: сканер читает синтаксис, запятая внутри строки ничего не заканчивает |
| `source.plain-code-is-left-alone` | сниппет, уже являющийся программой | прочитан | не тронут: разворачивают стори, а не код |
| `source.no-orphan-hint` | стори, из которой нечего извлечь | сниппет | пусто, а не одинокая строка `mount` |
| `source.keeps-a-story-without-parameters` | стори без параметров | сниппет | не тронут: «нечего вырезать» не повод переписывать |
| `code.a-paint-option-is-part-of-the-program` | стори с настройкой отрисовки | сниппет | настройка в коде: она меняет то, что читатель видит, значит она часть программы |
| `code.a-scene-option-that-is-not-a-paint-option-stays-out` | стори с настройкой витрины | сниппет | её нет: читатель не может её импортировать, и в его программе ей не место |
| `scene.dispose-retires-only-itself` | сцена, вытесненная следующей | закрыта | общие ресурсы не тронуты: закрывшая за собой шину сцена гасит дерево той, что стоит на экране |
| `locales.prose-is-a-bundle-key` | текст страницы | прочитан | это ключ, а не строка: проза, впечённая в стори, не может пойти за переключателем языка |
| `locales.lines-join` | абзац из нескольких строк | собран | склеен так, как читается, а не как записан |
| `controls.a-transform-is-exact-on-the-handles` | вставленный путь и рычаги построителя | применены | точки И управляющие точки преобразованы одинаково: криво преобразованная кубика выглядит как чужая форма |
| `controls.every-record-control-names-its-field` | двадцать полей записи против их объявлений | у каждого спрошена секция панели | у всех `surface`, и у каждого поля объявление ЕСТЬ. Раньше секцию нёс один `fill`, а `stroke` — тумблер, от которого зависят пятнадцать соседей — не был объявлен вовсе: Storybook вывел его из дефолта, и он приехал без описания, типа и заголовка |
| `controls.every-word-a-control-names-exists` | каждый литерал `arg.*` в файлах стори | сверен с бандлом chrome | все на месте. Пропущенное слово не падает: бандл отдаёт сам ключ, и строка приезжает с описанием `— arg.headSurface` — контрол без описания там, куда читатель как раз и идёт за описанием. Проверка локалей это не ловит: оба языка полны одинаково |
| `controls.a-literal-colour-beats-the-token` | и токен, и литерал | запись собрана | побеждает литерал. В этом и урок: токен идёт за темой, литерал — нет |
| `controls.an-untouched-override-is-its-base` | база со всеми необязательными частями (картинка, пунктир, свой cap) и пустая панель оверрайдов | срез слит | равен базе; одно заданное значение меняет ровно одно поле, а трёхпозиционный переключатель дотягивается до тумблеров записи в обе стороны. `Restyle` ставит базу рядом с клоном, и поле, пропущенное слиянием, невидимо на базе с дефолтами |
| `controls.an-untouched-pose-is-not-printed` | плоские контролы позы с дефолтами, затем по одному заданному полю | собраны поля и сниппет | пусто и `{}`; одно значение — одно поле, половина позиции пишет весь `at`, а нейтраль масштаба — ЕДИНИЦА: сборщик, меривший всё нулём, печатал бы нетронутый масштаб на каждой позе и прятал заданный ноль |
| `controls.a-prefixed-pose-reads-its-own-rows` | две целые позы на одной панели под префиксами `hand` и `card` | сняты срезы | каждый читает только свои строки: срез, задевший соседа, надел бы на один узел поворот другого, и ни одна сцена не сказала бы, который врёт |
| `controls.a-long-list-is-not-a-type` | пикер поверхностей, 92 записи | прочитан значок типа | `one of 92`, а не стена имён: значок стоит ПЕРЕД фразой о том, чего поле стоит, поэтому развёрнутый список выталкивает за край строки ровно то, за чем читатель пришёл — а сам список и так весь в пикере под ним |
| `controls.a-type-badge-is-not-guessed` | контроль каждого вида | прочитан значок типа | взят С КОНТРОЛЯ: шестьдесят подписей — шестьдесят шансов написать `number` рядом с текстовым полем, и никто не заметит |
| `shell.motion-speed-is-a-header-global` | шапка каталога; global `motion` (лесенка 0 · ½ · 1 · 2), `preview.apply` | переключение | `motionSpeed` уезжает в `viewer` настроек каталога и в `host.setViewer` каждой сцены — плоскость зрителя, не аргумент стори; e2e-ступень `checks-run-only-when-asked` гоняет Tests/Motion, где скорость задана третьим аргументом сцены |
| `scene.touch-toggle` | сцена, слой пальца выключен | кнопка нажата; наведение без нажатия, затем палец вниз и в сторону, второй палец, `pointerup` в ОКНЕ; кнопка отжата | наведение не оставляет метки — рука это прижатый палец; нажатый рисует кольцо и везёт его за собой, два пальца — две метки; конец жеста слышен на окне, иначе метку нельзя поднять; отжатие гасит слой и забывает руки. Вьюер при этом НЕ тронут: кит не знает, что у точки есть рука |
| `map.walls-inset-by-the-piece` | карта 8×8, карта 1×1.4 | посчитаны стенки | `±3.5 / ±3.3` — карта, ужатая на СОБСТВЕННУЮ половину фигуры: стенки клампят якорь, а за борт не должно выйти тело |
| `map.walls-count-the-pop` | та же карта, перенос держит подъём 1.06 | посчитаны стенки | граница ближе ровно на подъём: через линию глаз видит НАРИСОВАННОЕ тело, и без этого за край уходит те самые шесть процентов |
| `map.walls-never-turn-inside-out` | фигура вдвое больше карты | посчитаны стенки | коробка схлопывается в середину, а не выворачивается: при `x0 > x1` кламп отдаёт фигуру той границе, что применилась последней |
| `map.a-piece-with-no-box-is-a-point` | узел без `Bounded` | посчитаны стенки | стенки самой карты: отсутствие коробки это «нет размера», а не падение внутри отладочной сцены |
| `map.carries-four-pieces-of-three-kinds` | построена карта жестов | пересчитаны фигуры | четыре фигуры, среди них конь и кубик: перенос, который всю жизнь держит карту, учит карте, а не переносу |
| `map.drop-feel-is-read-off-what-the-piece-is` | карта жестов, у фигур спрошено падение | сравнены три ответа | карта самая медленная и не отскакивает вовсе, кубик отбивается сильнее резной фигуры, а резная не отскакивает вовсе — десятая процента, то есть «никак», записанное числом (не нулём, чтобы ПОРЯДОК троих всё ещё что-то говорил), а падают тяжёлые примерно одинаково — их различает посадка. Ответ берётся из МОДЕЛИ (`Rollable`, `Flippable`), а не из имени: по id это был бы список, в который пятую фигуру надо не забыть дописать, и `guard.id-is-opaque` его всё равно запрещает ; и у СТЕНКИ своё число: кубик у бортика живее всех, резная фигура мертвее всех, а карта, мёртвая на сукне, от борды всё равно возвращается — одним числом эта пара невыразима |
| `map.the-last-dropped-is-on-top` | стол с тремя фигурами | поднята первая, затем уже верхняя, затем средняя; и одинокий узел без владельца | поднятая уходит в конец списка, уже верхняя не трогается, ничего не теряется и не дублируется, у бесхозной ошибки нет. Это МЕСТО в списке, а не высота: равный `z` держит порядок дерева, а высотой каждый дроп поднимал бы стопку всё дальше от сукна навсегда |
| `map.the-stacking-desk-opens-with-nothing-touching` | построен стол стекинга | пересчитаны роды и кучи | шесть карт, шесть фишек, кубик — и ни одна не касается другой. Касание тема страницы, поэтому открываться без него она обязана: стол с готовой кучей учит куче, а не тому, как она заводится |
| `map.a-heap-is-one-kind-and-transitive` | три фишки в ряд, у которых крайние не сходятся; и фишка поверх карты | кучи | тройка — одна куча (транзитивно); фишка с картой не кучкуются. ЧТО с чем может касаться — правило стола, а не набора: набор отвечает на геометрию и останавливается |
| `map.a-handle-stands-under-the-middle-of-what-the-heap-covers` | стол без касаний; затем две фишки сдвинуты; затем разведены обратно | ручки | без касаний ручек нет (одинокая фигура не куча, а ручка под ней была бы контролом без действия); у пары ровно одна, по центру общего периметра и НИЖЕ его нижней кромки; разошлись — ручка снята: непере­рисованная висит под сукном, с которого куча ушла |
| `map.a-handle-is-never-the-same-node-twice` | две кучи, перерисовка ручек дважды, затем первая куча разведена | имена ручек | ни одно имя не повторяется, и уцелевшая не наследует имя ушедшей. Ручка — КАРТИНКА кучи, а не вещь на столе: названные по месту в списке, две ручки меняются именами, как только куча между ними исчезает, — часы видят один id со съехавшей позой покоя и ВЕЗУТ его туда, отчего все оставшиеся вкладки едут друг за дружкой, а новая вылетает из чужого места. Названная заново — узел, которого часы не видели: рисуется на своём месте и ниоткуда не летит |
| `map.a-lifted-heap-hangs-by-its-bottom-centre` | карты и фишки со стола стекинга | посадочные места поднятой кучи | нижняя кромка первой фигуры стоит ВЫШЕ вкладки, а не поверх её середины: ручка под кучей, а не сквозь неё; шаг ровный и вверх по стеклу; у фишки, которая ниже карты, своё место — половина СВОЕЙ фигуры, а не одно число, подошедшее тому роду, что записали первым ; и ТОЛЩИНА КУЧИ — свойство стопки, а не сумма карт: три карты держат полный шаг, тридцать при том же шаге дали бы почти целую карту разлёта и колода поднималась бы веером, поэтому шаг ужимается под потолок и куча остаётся кучей, а не плоскостью |
| `drag.settled-sees-the-written-tree` | сцена с картой, полный жест: нажал, увёл, отпустил | что видит `onCarry` c `done` и что видит `onSettled` | у руки в отчёте место, где карта БЫЛА, — это и есть перенос; у `onSettled` — где она теперь живёт, и вызывается он ровно раз за жест. `done` у переноса это конец РУКИ, а он наступает раньше, чем решён дроп, не говоря о записи: сцена, перерисовывающая что-то из дерева там, читает вчерашние места — ручка под кучей, которой там уже нет ; и он называет, ЧТО именно приехало: «что только что легло» — другой вопрос, чем «что лежит на столе», и сцене, кладущей новое сверху, нужен первый |
| `map.a-dropped-heap-pours-rather-than-slabs` | ран из ручки и двух фишек; он же из одной; пустой | порядок и задержки отпускания | ручки среди падающих нет (контрол не падает, он перерисовывается там, куда фигуры легли); низ стопки уходит первым, следующая на шаг позже — чтобы легла НА уже упавшее, а не под него; ран из одной без задержки вовсе, то есть все прочие страницы полки не изменились. Разом — куча приходит плитой; слишком врозь — это уже не одно падающее, а несколько сброшенных по очереди ; и у ПЕРЕЛИВА своя длина, сколько бы в нём ни было: пятеро держат полный шаг, тридцать шесть при том же шаге читались бы как очередь, которую ждёшь, поэтому шаг ужимается под потолок — большая куча льётся чуть дольше малой, а не в тридцать раз дольше. Та же форма, что и у толщины стопки, и по той же причине |
| `map.a-card-is-put-down-and-a-chip-is-dropped` | карта, фишка и кубик со стола стекинга; и переопределения по родам | способ покидания руки и веса | у карты `settle`, у фишки и кубика `fall`; читатель может поменять любой род, не задев соседний; веса при этом те же — из чего вещь сделана, не то же, что она делает. Не противоречие: карта, положенная на сукно, это «положил», а фишка, брошенная на него, — приземляющаяся вещь. `settle` ещё и тихий: ничего не бросается, значит нечего планировать и нечего предварительно переупорядочивать — оттуда и бралось мерцание ; у кубика третий способ — `roll`: отпущенный кубик катится ВСЕГДА, а не только если его сильно бросили (он для этого и есть, а упавший плашмя и оставшийся лежать был бы фишкой), и этот способ открыт только вещи с ГРАНЯМИ. Куча карт при этом не подаёт ни одного полёта — форма бага, за который уже заплачено: всё, что сцена вешает на приземление, для такой кучи не случается никогда |
| `map.a-roll-is-brisk-and-does-not-outstay-it` | поворот кости и её собственное торможение | длительность, число граней и граней в секунду | катится меньше двух секунд и дольше восьми десятых (не рывок), показывает больше двенадцати граней, больше восьми в секунду — размазня, каковой катящаяся кость и является. Грани считаются с СОБСТВЕННОГО поворота тела, поэтому скорость покупает обе половины разом; чего она покупать не должна — длительность, оттого торможение своё и круче стольного |
| `map.the-pictures-are-warmed-by-asking-for-them` | узлы прогрева и стол, который их держит | их род, места и участие в кучах | по узлу на каждую зарегистрированную картинку, все с родом `warm` и все ЗА пределами карты; стол по-прежнему держит ровно те фигуры, о которых говорит, и куч они не образуют. Художник грузит текстуру, когда ПЛАН впервые просит её нарисовать, и до её прихода слой не рисует ничего — оттого кость и заикается на первом же броске. Расшифровать картинку руками не помогает: у художника свой кэш, и наполняется он только своими загрузками, то есть планом |
| `map.a-slow-hand-puts-down-and-a-fast-one-throws` | карта и фишка, отпускание на нуле и на пороге броска | летит ли | карту на месте кладут, на пороге бросают; фишка летит и без помощи руки. `settle` — это СКОРОСТЬ, а не запрет: прочитанный как «эту вещь нельзя бросить», он означал бы карту, щелчком отправленную через стол и просто появившуюся там, где палец встал |
| `map.not-everything-leaves-a-hand-at-the-hand-s-speed` | доли скорости и трение у карты, фишки и кости | сравнение | у фишки доля меньше, чем у карты и кости, и трение больше — она мала и тяжела для своего размера, её перестают толкать в момент отпускания; у карты целое лицо на сукне, она летит туда, куда послали. Один коэффициент на всех делал самую лёгкую вещь на столе самой быстрой — обратное тому, что чувствует рука |
| `map.a-piece-in-flight-is-in-no-heap` | две касающиеся фишки, одна из них объявлена летящей; и третья, дотягивающаяся до оставшейся | кучи и ручки | летящая не в куче: остаётся одна фигура, а одна — не куча, и ручки нет; с третьей на столе куча снова есть, но уже без летящей. Дерево сажает брошенную там, где рука её выпустила, — касающейся того, с чем она лежала, — поэтому куча, построенная по одному дереву, посчитала бы её, и повторный хват за ручку выдернул бы карту из её собственного полёта |
| `map.a-handle-in-a-hand-is-not-redrawn` | две кучи, перерисовка ручек с указанием удерживаемой; и без него | что осталось на столе | удерживаемая — ТОТ ЖЕ узел и всё ещё на столе, соседняя сделана заново, как и всегда; без исключения удерживаемая исчезает, и рука остаётся с мёртвым id. Прочие вкладки нарочно делаются заново — этим они и не наезжают друг на друга, — но та, что под пальцем, принадлежит жесту до его конца: заменённая на ходу, она новый узел, которого рука не брала, и держимое пропадает у неё из-под пальца. Причём хватает любой посадки где угодно на столе, чтобы перерисовка случилась |
| `shell.a-desk-is-not-a-document` | построенная сцена | оболочка и правила стиля | вся сцена помечена, а правила лежат в ТАБЛИЦЕ: `touch-action`, `user-select` с префиксом и без, `-webkit-touch-callout`, `overscroll-behavior`. Набор уже говорит это самому холсту, и этого хватает долгому нажатию, попавшему в карту; не хватает тому, что попало на пару пикселей мимо — подписи тулбара и заметка в углу это текст, телефон предлагает текст выделить, и iOS открывает лупу поверх всей сцены, то есть линзу поверх той самой карты, которую тащат. Таблица, а не инлайн, по одной причине: инлайновое объявление разбирает получивший его браузер, и незнакомое ему свойство ВЫБРАСЫВАЕТСЯ, — а то единственное, что лупу и глушит, незнакомо всем, кроме телефонов, ради которых оно и написано |
| `drag.the-zone-a-run-came-from-is-asked-with-the-rest` | сцена, чей `zoneAt` отказывает всем | полный жест | спрошена ДВАЖДЫ и оба раза о ФИГУРЕ, а не о столе: раз на ходу — зона, которая возьмёт, зажигается, пока ещё можно прицелиться (`aimAt`, с откатом на этот же ответ), и раз на отпускании; отказанный релиз — обычный, фигура остаётся там, где палец её оставил. Зона, тянущаяся за картой, которую только что отдала, — это зона, из которой ничего нельзя вынуть: вытащи карту и отпусти — ты всё ещё в её притяжении (ты всегда в нём, в этом притяжение и состоит), и она забирает карту обратно. При двух зонах рядом карта просто скачет между ними, и положить её на стол негде. Проводке этого не знать: какой зоне принадлежит релиз — ответ сцены, и «не этой» тоже; здесь пришпилено, что сцену СПРАШИВАЮТ, дав ей всё, чем ответить, — и точку, и фигуру |
| `drag.the-zone-that-would-take-it-says-so-while-there-is-time-to-aim` | зона с коутом прицела; сцена, чей `zoneAt` отвечает то ей, то никем | палец ведёт мимо, потом над ней, потом снова мимо, потом отпускает над ней | молчит · надела `wash` · в никуда — сняла сразу · после отпускания снята. Зона тянется дальше собственной границы, так что граница не отвечает на вопрос «я уже долетел»: неся карту через сукно, игрок имеет только свою догадку и узнаёт о промахе, промахнувшись. Зажигает ТОТ ЖЕ ответ, которым решится отпускание: свет со своим представлением о «достаточно близко» хуже, чем никакого, — он обещает зону, которая потом карту не возьмёт, а читатель верит свету, а не исходу. Сторож проверен отказом дважды: не зажглось и не погасло |
| `drag.the-hand-is-measured-where-the-hand-is` | сцена, забирающая отпускание; палец идёт 75 px каждые 50 мс, затем отпускается сразу; тот же жест, но перед отпусканием пауза длиннее `FLING.maxGap` | что за скорость получает сцена | 1500 пикселей в секунду (без волоса — берётся половина каждой новой выборки, так что ровная рука приближается и не достигается: один дёрганый кадр не должен стать броском), и НОЛЬ после паузы. Скорость пальца по экрану просто известна — две точки и время между ними; читать вместо неё скорость догоняющей пружины и выводить обратно через камеру значит уйти на три пересчёта вглубь, и каждый из них — место ошибиться в разы, которых на стекле не видно. Пауза — рука в покое, а не медленная: пронести карту, задержать над местом и отпустить — это класть, а не бросать, и оно не должно наследовать скорость, с которой рука туда шла. Проверен отказом дважды: без выборки на отпускании (наследует 1494) и без разрыва по паузе (747) |
| `drag.a-taken-release-never-reports-the-hand-letting-go` | сцена, чей `onRelease` отвечает «взял»; и сцена, чей отвечает «нет» | что услышал `onCarry` | в первом случае только «едет» и никогда «отпустил», во втором — «отпустил» последним. Отчёт о завершённом переносе приходит из СОБСТВЕННОГО дропа проводки, а взятый сценой релиз до этой строки не доходит вовсе. Всё, что стол на этот отчёт повесил, просто не происходит — а на общем столе это значит, что чужой экран продолжает держать карту, брошенную минуту назад: поднятой, накренённой и следующей за пальцем, который давно отпустил |
| `drag.a-release-the-scene-took-is-never-announced` | сцена, чей `onRelease` отвечает «взял»; и сцена, чей отвечает «нет» | что услышала стори | в первом случае только «взял», во втором только `onSettled`. Сцена, забравшая дроп, знает, что сделала, и рассказывать ей об этом потом значило бы, что проводка выдаёт чужую работу за свою. Пришпилено ради ловушки с другой стороны: всё, что стол вешает на `onSettled` — выравнивание того, что взяло место, перерисовка ручек, — на взятом сценой релизе не происходит ВООБЩЕ, и сцена обязана сделать это сама в конце собственного падения |
| `drag.a-gesture-reports-itself-once` | сцена с картой: нажал-отпустил на месте; нажал-подержал-отпустил; нажал-увёл-отпустил | что услышала стори | тап, дроп, дроп — по одному ответу на жест, и фигура после тапа ровно там, где была. Тап и перенос это ОДИН жест до самого конца: тот же палец на той же фигуре, различает их только путь и время. Двумя слушателями сработали бы оба, и сцене пришлось бы их рассуживать, — ветка, которой шов и существует не иметь. Тап при этом не дроп: фигура никуда не ходила, класть нечего |
| `drag.the-sitter-goes-where-the-plan-says` | клетка-приёмник с `Displacer` («взятый — в tray»), на ней сидит фигура; рядом пустой tray; тянем на клетку другую | полный жест | пришедший занимает клетку, СИДЕВШИЙ уходит в tray и ставится В НЕГО, на первое место, а не остаётся со своей доской-посадкой. Место считается среди тех, кого может взять РУКА (`Draggable`), а не среди всех детей: в tray заранее лежат три «места»-контейнера и двадцать неприкасаемых «пылинок» (прогревочные узлы стола — по одной на картинку, их сотня), и первый взятый всё равно на первом месте; сотня пылинок сажала первого взятого на 102-е место — седьмой ряд сукна, то есть НА доску, на a4; шаг ряда — собственный размер фигуры (rect 1 → −0.5,−0.5), а не доля зоны: доля от стола в 14 единиц давала четыре колонки, вторая из которых — на доске. План носит это с тех пор, как у кита появились места (`MovePlan.occupied`), и его собственный комментарий зовёт это «непрозрачными данными плана для СРЕДЫ ВЫПОЛНЕНИЯ»: шов решил, кто-то обязан СДЕЛАТЬ. Никто не делал. Доска, объявившая, что фигура, вставшая на занятую клетку, берёт сидящего, получала двух на одной клетке — это не взятие и даже не баг, который видно, пока второй не пойдёт. Делается таблицей, а не ветвлением: поведение не читает сорт (`guard.no-kind`), и ответ кита на «этим может быть несколько вещей» везде один — имя, найденное в реестре, как и сами записи `occupied`, породившие этот исход. Проверен отказом дважды: без вызова и без посадки |
| `drag.capture-custom-landing` | Displacer с `capture(tray, landing)`, где landing зарегистрирован в реестре посадок | полный жест | если результат содержит `landing` и запись найдена, координаты берет из функции реестра, а иначе скручивается в ряд от угла |
| `drag.a-place-the-hand-has-left-lets-its-light-go-slowly` | две зоны с коутом прицела; сцена, чей `zoneAt` отвечает то одной, то другой | палец над первой, затем над второй, пауза, затем отпускание | достигнутая горит сразу и в полную; покинутая ещё горит, через 120 мс заметно тусклее, через 500 — нуль; та, что под рукой, держит; отпускание — сразу нуль. Свет, гаснущий в тот же миг, когда рука пересекла черту, читается мерцанием на сетке — шестьдесят четыре места, и каждое жёсткий край, который ловит глаз, — а мерцание под движущейся рукой выглядит как зависание. Так что место, которое рука только что ПОКИНУЛА, держит свет и отпускает его за несколько шагов, пока достигнутое горит уже сейчас. Лишь рука, которая УШЛА, забирает свет разом: зона, горящая над пустым сукном, — ложь. Шагов шесть по таймеру, а не кадр за кадром: каждый шаг — стол, разложенный заново, а глаз не отличит шесть шагов четвертьсекундного затухания от шестидесяти |
| `map.a-deck-is-a-heap-and-nothing-else` | стол с колодой: 36 карт, шесть в открытую | роды, кучи и переворот | тридцать шесть карт, шесть лицом вверх; закрытые образуют РОВНО одну кучу, открытые не касаются никого; последняя карта кучи стоит в детях позже первой, то есть рисуется поверх, — в неё палец и попадает. Про колоду нигде ничего не написано: это тридцать карт на одном месте, то есть куча, то есть все правила, которые у стола уже есть |
| `map.every-desk-can-draw-a-handle` | каждый стол построен ПЕРВЫМ в чистом реестре поверхностей | зарегистрирована ли поверхность его ручки | зарегистрирована у всех. Неизвестное имя поверхности план ПРОПУСКАЕТ и не бросает (одна плохая ссылка не должна ронять сцену и прятать всё остальное) — поэтому ручка, чью картинку записал другой стол, рисуется в ничто, и страница выглядит ровно так, будто стекинг выключили. Реестр общий на процесс, оттого и «первым в чистом»: иначе на вопрос ответил бы сосед |
