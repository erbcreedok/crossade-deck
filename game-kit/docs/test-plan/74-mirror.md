## UNIT · mirror — чужая рука на своём экране

`vitest + jsdom + фейковые часы` · 2 кейсов, расписано 2

`follow` из `game-kit/src/render/mirror.ts`: как экран показывает руку ДРУГОГО игрока — карри, начатый
один раз и потом только ведомый, и курсор чужой руки как колбэк (`onCursor`), а не DOM-элемент.

| id | Дано | Когда | Тогда |
|---|---|---|---|
| `mirror.the-far-screen-moves-the-piece-the-near-hand-is-holding` | сцена с ручными часами и рисовальщиком-счётчиком, `follow` с одним человеком и якорем, затем с `done` | что сцена рисует | до переноса у фигуры нет наложения; после `follow` — есть (она едет за якорем), и КАДР ЗАПРОШЕН; сам шаг ничего не красит — красит только кадр, и после 60 кадров поза стоит в новом якоре, а не в точке захвата; курсор чужой руки сообщён через `onCursor`; после `done` — ничего не зеркалится, курсор спрятан (`onCursor(undefined, undefined)`) |
| `mirror.two-screens-two-hands-moving-one-does-not-affect-the-other` | две независимые сцены со своими часами | обе руки двигают свои фигуры через `follow` | движение первой руки не сбивает вторую: на обеих сценах обе фигуры едут к своим якорям |
