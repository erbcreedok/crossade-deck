## UNIT · the scene plan

`vitest (headless, no WebGL)` · 75 кейсов, расписано 75

Геометрия — чистая функция, рендерер только превращает ответ в объекты. Настоящий Pixi в jsdom не
живёт, поэтому всё, что решено ВНУТРИ рендерера, непроверяемо по построению: правило, проскочившее
мимо этой секции, не сторожит никто.

| id | Дано | Когда | Тогда |
|---|---|---|---|
| `grid.off-by-default` | сцена | слой не просили | пусто: линейка — то, за чем тянутся, а не то, с чего страница открывается |
| `grid.one-line-per-unit` | 800px при 100px на юнит | построена сетка | девять вертикалей с шагом ровно в юнит — всё утверждение слоя |
| `grid.is-ruled-from-the-origin` | то же | прочитаны координаты линий | линии пересекаются в центре вида: сетка от угла ставила бы ноль линейки туда, откуда никто не меряет |
| `grid.tenths-are-lines-too` | сетка при 100px на юнит | прочитаны линии десятых | они через весь вид, тоньше юнитовых, своим токеном и никогда поверх целой линии. Штрихами на осях это была шкала, которую глазом переносили к фигуре; линиями — сетка, по которой читают там, где фигура стоит |
| `grid.tenths-go-when-they-stop-being-readable` | эталон 50px | построена сетка | одни целые линии: десятая в 5px — чернила, а не сведения |
| `grid.follows-the-etalon` | эталон 50 против 100 | построены обе | у мелкого линий больше: сетка мерит юниты, значит движется вместе с ними |
| `grid.own-ink-and-never-the-box-outline` | сетка и контур коробки | прочитаны метки | у сетки свой токен. В чернилах контура линейка была бы того же цвета, что и предмет, который она мерит, и включать оба стало бы бессмысленно |
| `grid.too-fine-to-read-is-not-drawn` | эталон 3px и 0 | построена сетка | пусто: ниже нескольких пикселей это заливка, прячущая сцену, а не сетка |
| `scene.grid-toggle` | две сцены на странице | нажата сетка у одной | включилась у неё одной, и второй слой при этом не включился |
| `e2e.grid-reaches-the-glass` | `Bounded/Square` | сетка включена и выключена | картинка изменилась и вернулась. План вправе быть прав и не доехать до холста — решает только стекло |
| `marks.off-by-default` | узел с коробкой, отладка выключена | отметки посчитаны | пусто: инструментальный слой появляется потому, что его попросили, а не сам |
| `marks.a-box-becomes-visible` | тот же узел, отладка включена | отметки посчитаны | одна отметка. Ради этого слой и заведён: `Bounded` не рисует ничего, и до кнопки коробку приходится принимать на веру |
| `marks.the-origin-is-drawn` | узел с многоугольником, смещённым от нуля | включён отладочный слой | перекрестие стоит в НАЧАЛЕ, а не в середине коробки. `at` ставит именно эту точку, и поворот будет вокруг неё; пока её не рисовали, о смещении сообщал бы только объект, улетевший при первом повороте |
| `marks.no-box-no-mark` | узел без `Bounded` | отметки посчитаны | не обведён — обводить нечего |
| `marks.a-rect-is-four-corners` | `rect 2×1`, юнит 100 | отметка посчитана | четыре угла в пикселях вокруг места, где узел стоит |
| `marks.a-circle-is-a-polygon` | `circle{r:0.5}` | отметка посчитана | многоугольник, все точки на радиусе. Форма разрешается в ТОЧКИ здесь, поэтому рендерер не читает сорт — `guard.no-kind` переживает появление второго рисующего |
| `marks.a-poly-keeps-its-points` | треугольник | отметка посчитана | его собственный контур, а не описанный прямоугольник |
| `marks.follow-the-layout` | два ребёнка под `row` | отметки посчитаны | обводки стоят там, куда РАСКЛАДКА поставила узлы |
| `plan.a-box-alone-draws-nothing` | Bounded без Surfaced | план построен | пусто: ни бледной рамки, ни отладочного прямоугольника. Коробка настоящая и невидимая |
| `plan.surfaced-draws-one-quad` | Bounded + Surfaced | план построен | ровно одна фигура |
| `plan.the-root-sits-in-the-middle` | один узел, вьюпорт 800×600 | план построен | центр `400,300` — на это опирается каждая страница каталога |
| `plan.units-become-pixels-once` | `plate` (0.03 бордер, 0.08 радиус), юнит 100px | план построен | `3` и `8` пикселей: конвертируется КАЖДАЯ длина, а не только размер |
| `plan.an-unregistered-record-is-skipped` | ссылка на незарегистрированную поверхность рядом с исправным узлом | план построен | пропущена только висящая ссылка; исправный узел нарисован |
| `plan.a-record-without-a-stroke-still-fills` | `plate` и `bare` | оба построены | у второго нет бордера, габариты те же |
| `plan.restyle-reaches-every-node-at-once` | три узла на `plate` | запись перерегистрирована без бордера | бордер исчез у всех троих, и ни одна коробка не сдвинулась |
| `plan.z-orders-the-paint` | дети с z=5 и z=1 | план построен | порядок по высоте, равные — в порядке дерева |
| `plan.the-contour-comes-down-as-points` | узел с формой | план построен | контур приходит ТОЧКАМИ: рендереру не на что ветвиться, и `guard.no-kind` переживает второго рисующего |
| `plan.the-radius-rounds-the-contour-not-the-box` | запись с радиусом | план построен | скруглены точки контура, коробка цела: радиус — дело краски, а не занятого места |
| `plan.a-dashed-stroke-arrives-already-cut` | запись с пунктиром | план построен | список полилиний в пикселях. Резать в рендерере значило бы резать там, куда тест не дотянется |
| `plan.layers-come-down-in-order` | запись в несколько слоёв | план построен | снизу вверх, тем же порядком, каким записаны |
| `plan.what-is-in-a-screened-node-is-held-with-it` | `bar` (`Screened`, `Oriented: viewer`) с двумя детьми по ±0.75 | план при зуме 1, зуме 2, повороте вида на 90° (`rotation: 90`) | дети держат пиксели, как и бар; расстояние между ними на стекле не меняется с зумом (ряд остаётся рядом, а не сбивается в кучу / не расползается по столу); при повороте вида дети встают вместе с баром — ряд по-прежнему вдоль x стекла. Карта на стекло наследуется от владельца: «карта владельца, затем шаг от его позы к моей» |
| `plan.a-screened-node-keeps-its-size-on-the-glass` | обычный узел и узел со `Screened`, вид при зуме 1 и при 2 | нарисованный масштаб и место | при зуме 1 они одинаковы; при 2 обычный вырос вдвое, а контрол ни на сколько — и остался НА СВОЁМ месте, потому что отмена масштаба идёт вокруг его собственного начала. Всё, что лежит НА столе, меряется юнитами и растёт с картинкой; ручка меряется пикселями, потому что она под палец, а палец с картинкой не растёт ; и через НАСТОЯЩУЮ камеру, чья матрица складывает эталон с зумом; а с границами (`min 0.8`, `max 1.4`) при зуме 4 перестаёт расти на потолке, при 0.2 — уменьшаться на полу, а между ними просто едет с видом. «Не меняется вовсе» верно только в середине: далеко наружу ручка, державшая пиксели, подавляет ту самую вещь, ручкой которой она является, а далеко внутрь становится точкой на картинке одной карты |
| `marks.follow-a-node-in-flight` | узел с коробкой, оверрайд на два юнита вправо | обводка и перекрестие с оверрайдом и без | нарисованы там, где узел НАРИСОВАН, а не где покоится. Перенос — это оверрайд, а не запись в дерево: по дереву обводка осталась бы на месте, пока поверхность едет за пальцем, и читатель смотрел бы на один элемент в двух положениях |
| `marks.follow-the-field` | узел с отметками | план построен | отметки идут за полем, а не за отдельной настройкой рядом |
| `plan.baking-keeps-the-stroke-the-width-it-was-authored` | запечённый узел с масштабом | обводка прочитана | ширина та, что объявлена: это `vector-effect: non-scaling-stroke` и вся разница между режимами |
| `plan.baking-cuts-the-dashes-again` | запечённый узел с пунктиром | штрихи прочитаны | нарезаны заново по сложенному контуру, а не отображены — иначе масштаб растянул бы узор, который по определению не тянется |
| `plan.a-lifted-container-lifts-its-children` | контейнер z=10, ребёнок z=1 | ребёнок в плане | 11 — подняли пачку, поднялась вся пачка |
| `plan.a-raised-node-paints-last` | квады z=0 и z=5; `raised` на нижнем; на обоих | план построен | полёт бьёт высоту: поднятый рисуется ПОСЛЕДНИМ, его `z` в кваде — прежняя правда покоя; двое поднятых держат свой порядок по высоте — подсказка поднимает группу, не перемешивая её |
| `plan.pieces-at-one-height-do-not-shadow-each-other` | кастер и высокая (z=9) плашка рядом | план построен | порядок: сукно, тени лежащих, лежащие, тени поднятых, поднятые. Фигуры на ОДНОЙ высоте не затеняют друг друга — они касаются, а между касающимися вещами нет зазора, в котором жила бы тень; нарисованная так, будто он есть, колода из тридцати шести впечатывает в себя тридцать шесть тёмных кромок и выходит чёрной плитой — сперва на сукне, потом ровно так же в руке. Фигура НАД другой затеняет её: там зазор и есть всё дело, и это единственное, что говорит глазу, что она оторвалась от стола. Понадобилось три захода: поднятая одним проходом под всё — тень не видна над крашеным сукном и уходит ПОД карты, над которыми висит; оставленная на своём месте для всех — стопка затеняет себя; разделённая только на лежащих и поднятых — стопка затеняет себя В РУКЕ. Падение вниз-влево от фонаря сверху справа; у квада id отбрасывающего с суффиксом, который ничто не разрешает: не взять пальцем, не запечь, не спутать с фигурой |
| `plan.a-later-piece-at-the-same-height-draws-after` | узел, добавленный ПОЗЖЕ второго ребёнка, тот же `z` | `scenePlan` построен | id узла, добавленного позже, стоит В КВАДАХ ПОСЛЕ первого — новая карта рисуется НАД старой, а не под ней |
| `plan.a-nested-container-is-not-ground` | сукно и плашка-контейнер с лицом-контейнером внутри, оба под НЕ-поверхностным корнем | план построен | сукно раньше плашки, плашка раньше лица: `grounds` — это `id` КОРНЯ и только он, а не всякий узел с `Container` — иначе плашка и лицо (оба контейнеры) сами объявлялись «сукном» и тонули под настоящим сукном хаба, у которого `Container` нет вовсе |
| `plan.a-caption-rides-its-nodes-quad` | узел с поверхностью и `Labeled({label:"ab"})`, линейка 10px/символ | план построен | у квада есть `text` со строкой «ab»; перо на −10 — пиксели в СОБСТВЕННОМ пространстве узла, том же, что и контур: художнику не нужна вторая договорённость |
| `plan.a-caption-needs-no-surface` | узел с боксом и подписью, но БЕЗ `Surfaced` | план построен | квад есть, `layers` пуст, обводки нет: правило лестницы — что голый БОКС не рисуется, а не что слова не рисуются |
| `plan.no-ruler-no-caption` | тот же узел, план без `measure` и с ним | план построен дважды | без линейки `text` отсутствует — сцена бит-в-бит та же, что до появления текста (пропускаем, а не бросаем); с линейкой подпись появляется, и разница ровно в линейке |
| `plan.a-billboard-ignores-the-owners-turn` | поднос под 30° со `scale 2`, в нём два одинаковых жетона на одном месте — обычный и `Oriented({orientation:"viewer"})` | план построен | обычный несёт 30° подноса, билборд — 0°: накопленный поворот владельцев не наследуется ВООБЩЕ; место и масштаб при этом приехали по цепочке нетронутыми — кадр режет угол, а не раскладку |
| `plan.a-billboard-ignores-the-camera-s-own-turn` | камера повёрнута на 180° (`Camera.rotation`), обычный узел и билборд на одном месте | план построен с `rotation: c.rotation` | обычный узел несёт поворот камеры целиком, билборд — 0°: поворот камеры, зашитый в `view`/`toView`, снимается вокруг СОБСТВЕННОГО начала узла тем же приёмом, что и наклон (`standing`); место и масштаб не тронуты, без поворота камеры кадр не меняется |
| `plan.a-billboard-s-shadow-stands-with-it` | камера повёрнута на 180°, билборд с `ShadowCaster.picture` | план построен с `rotation` | тень билборда стоит под тем же углом, что и он сам (0°): тень — его собственный силуэт, снимает поворот камеры и наклон вместе с ним, а не лежит вторым, повёрнутым силуэтом |
| `plan.a-turned-piece-turns-its-silhouette-not-its-shadow` | фигура под 0° и под 60° | смещение тень−фигура сверено | вектор падения ОДИНАКОВ под любым углом (свету всё равно, как повёрнут предмет), а матрица тени поворот несёт — силуэт следует за нарисованной геометрией |
| `plan.a-held-piece-stands-by-its-fraction` | четыре одинаковые фигуры с тенью на одном месте, камера с наклоном 60°; одной `stood 0.5`, другой `stood 1`, третья билборд | построен план | половинная — в полтора роста лежащей (высота идёт долей), полная — ровно в рост билборда, посадка та же (распрямление вокруг своей точки); тени у всех троих лежат на сукне в высоту лежащей: что держит рука, ровно к ГЛАЗУ, а тень и контур посадки — факт про сукно |
| `plan.a-billboard-stands-out-of-a-laid-back-desk` | два одинаковых узла на одном месте, у одного `orientation: viewer`; камера с наклоном 60° | построен план, и он же без наклона | лежащий вдвое ниже, стоящий в полную высоту, и посадка у обоих ОДНА: распрямление идёт вокруг собственной точки узла, иначе каждая фигура, кроме центральной, уползала бы вверх. Без наклона оба узла — один и тот же узел дважды: кадр ничего не стоит там, где ничего не наклонено |
| `plan.a-shadow-under-a-camera-keeps-its-length-in-units` | кастер на столе, вид даёт КАМЕРА | зум вдвое, потом поворот на 90° | вынос тени вырос вдвое, а направление на стекле не шелохнулось: длина — в юнитах и меряется масштабом ДЕЙСТВУЮЩЕГО вида (по эталону она держала бы постоянный пиксель, и фигура на зуме сползала бы на собственную тень), а направление принадлежит кадру — лампа в правом верхнем углу КАДРА, и поворот камеры не обводит тень вокруг фигуры |
| `plan.height-deepens-the-shadow` | кастер с z=0 и z=3 | длина падения сверена | z — источник, длина тени — следствие: выше — дальше; на столе (z=0) остаётся волосок тени, иначе ничто не скажет, что фигура не нарисована на сукне |
| `plan.a-stack-casts-once` | стопка-кастер с картой-кастером внутри; та же карта отдельно | план построен | у стопки ОДНА тень — её собственная; отдельно стоящая карта кастит сама: кастование выведено из цепочки, не из флажка |
| `plan.a-shadow-lies-the-way-the-piece-does` | кастер 2×1 с override-позой: четверть оборота НА МЕСТЕ; масштаб 1.5; и отъезд на 3 юнита | тени сверены с покоящейся | тень повёрнута вместе с фигурой (`b`≈1 против 0) и при обороте на месте никуда не уехала; хоп-масштаб она берёт тем же порядком (×1.5); а за уехавшей фигурой уезжает на все 3 юнита — КАК лежит и ГДЕ лежит фигура тень повторяет одинаково |
| `plan.a-shadow-rides-a-body-across-the-desk` | кастер с override-позой в 3 юнитах от места: раз в `grounded` (высота 0), раз в `grounded` с высотой 0.6, раз без слова вовсе | тени сверены с фигурой | тень ПОД фигурой в любом случае — и со словом, и без него: незаявленная высота это отсутствие высоты, а не тень, брошенная на месте; выше — больше зазор (высота и есть зазор до тени, поэтому подпрыгивание читается) |
| `plan.a-shadow-is-under-its-piece` | кастер с override-позой: раз в `carried` (палец держит), раз без (клок несёт) | планы сверены с покоящимся | тень уезжает вместе с предметом в ОБОИХ случаях — предмет и его собственная тень не бывают в разных местах; рука остаётся единственной разницей, и она ДЛИНА, а не место: в захвате зазор между фигурой и тенью шире, чем при том же переезде на клоке |
| `plan.a-stacks-shadow-wears-its-content` | стопка-кастер со слотом 1×1.4 и картой глубоко внизу; пустая стопка | план построен | тень полной — обёртка СОДЕРЖИМОГО (объединение футпринта и посаженных детей в системе стопки): слот-тень под длинной колонной сказала бы, что карты парят; пустая — свой короб |
| `plan.the-hand-deepens-the-shadow` | кастер в `carried`, в `raised` и без | длина падения сверена | поднимает РУКА: держимому падение удлиняется на `lifted` и кончается вместе с жестом; `raised` сам по себе не удлиняет — там же и всё, что несёт клок, а летящая карта не стоит выше стола на дальнем конце пути, чем на ближнем |
| `origins.child-is-owner-plus-layout` | владелец в `2,0`, ребёнок в `1,1`, раскладка `free` | позиции посчитаны | `3,1` — смещения складываются |
| `origins.a-placing-layout-wins` | ребёнок с `at 50,50` под `row` | позиции посчитаны | раскладка переписала позу, а не сложилась с ней |
| `origins.deep-chain` | три уровня со своими смещениями | позиции посчитаны | каждый уровень добавил своё |
| `plan.substitute-children-are-drawn` | эффект подменяет узел на поддерево с ребёнком | план построен | ребёнок ПОДМЕНЫ нарисован: обход идёт по показанному узлу, не по авторскому дереву |
| `plan.original-children-are-not` | та же подмена, у исходника свой ребёнок | план построен | ребёнок исходника отсутствует: контент лица не просвечивает сквозь изнанку |
| `plan.substitute-children-have-transforms` | та же подмена | `transformsOf` построен | id детей подмены есть в карте: у каждой фигуры плана есть место |
| `plan.a-partial-layer-carries-its-clip` | слой с `part: 0.25`, бокс 1×2, юнит 100 | план построен | клип-прямоугольник нижней четверти В ПИКСЕЛЯХ: доля стала точками тут, а не в painter'е |
| `plan.a-whole-layer-has-no-clip` | слой без `part` | план построен | `clip` отсутствует: целый слой — не маска в полный рост |
| `plan.the-desks-lamp-sets-the-depth` | корень с `Lit({light, shadow: {base×2, opacity 0.9}})` vs корень без `Lit` | план обоих | длина падения вдвое больше стоковой, непрозрачность слоя тени 0.9 (сток — `DEFAULT_SHADOW.opacity`): коэффициенты — данные СТОЛА, не константы движка |
| `plan.a-caster-may-lay-its-own-drawing` | стол с лампой (`opacity 0.5`), зарегистрированный ассет `plan.knight-shadow` 1×1, фигура 1×1 с `ShadowCaster({picture})` | слой теневого квада | `image.src` — названный рисунок; `paint` пуст (краска под рисунком вернула бы коробку); `opacity` = лампы (0.5); картинка вписана в бокс — 100×100 px при unit 100. Пятно ниже НИЧЕГО не изображает; рисунок изображает ПРАВИЛЬНОЕ: план по-прежнему не читает альфу картинки — тень нарисовал тот, кто нарисовал фигуру |
| `plan.a-caster-may-lay-something-other-than-itself` | стол с лампой; одна фигура молчит про контур, вторая называет пятно (эллипс, сдвинутый к основанию) | какой формы вышли теневые квады | молчащая кладёт свою коробку без изменений; назвавшая кладёт ПЯТНО — вдвое уже и вчетверо площе коробки. Конь есть дыра формы коня в квадрате, и падает КВАДРАТ: прямоугольник размера, который глазу не с чем сопоставить, под фигурой, которой он явно не принадлежит. Честный силуэт можно только СНЯТЬ с рисунка, а общий пасс собирает маску контурами и кладёт снимок прямоугольником — это пробовали и заплатили: видно рамку вместо коня. Значит, отбрасыватель вправе сказать, что кладёт: пятно у основания ничего не изображает и потому не может изобразить не то. И никогда не контур «фигуры вообще» — это подделка, она даёт коню тень пешки в тот день, когда появляется второй род фигур. Проверен отказом |
| `plan.a-fall-of-nothing-is-no-shadow` | стол с лампой `base 0, perZ 0`, на нём отбрасыватель | план в покое и с ним же в `raised` | в покое слоя тени нет вовсе; поднятый отбрасывает. Темнота слоя — константа, так что отбрасыватель, которому лампа не даёт падения в покое, всё равно красил тень в полную силу ровно под собой: мазок под каждой фигурой на доске, читаемый как грязь, а не как высота. Высота — единственное, что тень говорит; когда сказать нечего, она молчит |
| `marks.badge-off-by-default` | узел с меткой | `scenePlan` построен политикой без `marks.badge` | квада `"mark"` (значка) нет вовсе — сам ореол при этом на месте: значок теперь второй, необязательный слой |
| `marks.badge-in-top-right-corner` | узел с меткой, политика `marks.badge: true` | `scenePlan` построен | квад слоя `"mark"` в правом верхнем углу коробки узла, залит цветом игрока, поверх иконка метки — только когда зритель сам попросил значок |
| `marks.policy-hides-mark` | узел с меткой, политика `showOwn: false` для автора | `scenePlan` построен | квада метки нет |
| `plan.private-seat-skips` | поддерево с `Private({access:["south"]})` и ребёнком внутри | `scenePlan` для зрителя `marks.me: "north"`, для `"south"`, и без места | для `north` ни узла, ни ребёнка нет; для `south` и для зрителя без места узел есть |
| `marks.a-glow-and-no-trail-and-no-badge` | узел с меткой и `from`, юнит 100 | `scenePlan` построен | квада-линии нет вообще; нет значка (политика по умолчанию); есть мягкое свечение — ЗАЛИВКА контура самой фигуры цветом игрока, полупрозрачная, с блюр-фильтром, шириной с коробку (200 px у 2×2), без обводки. Пунктир «откуда» и жёсткая обводка всей карты были громче всего на столе — метка должна шептать |
