## UNIT · грани позы покоя — что зона делает с прилетевшим

`vitest` · 23 кейсов, расписано 23

Поза покоя разбирается по ГРАНЯМ, и по каждой зона отвечает одно из трёх: **derive** (взять из
раскладки), **stamp** (навязать своё, историю игнорируя), **keep** (принять принесённое). Эталон
поведения — `docs/scenarios/pose.md`; здесь закрыт резолвер угла.

Три ответа — ЗАПИСИ РЕЕСТРА, не union из трёх имён: сценарий F требует, чтобы игра дописала
четвёртый (песочница с «открытым столом», покерная рука на вскрытии) одним `registerGrain`, а не
веткой в движке. Сторож на это — `guard.grain-is-a-record` в слое 15.

Карта помнит СВОЮ сторону, зона может быть перевёрнута целиком, а видит владелец их XOR. Кит этого
нигде не сторожит: `Flippable.turns` складывается по цепочке, значит чётность карты внутри
перевёрнутой стопки уже и есть XOR. Отсюда и модель закрытой колоды: перевёрнута ЗОНА, а не карты в
ней — вытянул в нетронутую руку, и лицо показалось, хотя карте никто ничего не писал.

Записи грани `side` отвечают, ЧТО ДОЛЖЕН УВИДЕТЬ ВЛАДЕЛЕЦ, а не какой бит писать: бит пишет
`setFacing` уже после смены владельца, и он сам подмешивает поворот зоны. Поэтому штамп читается
буквально — `up` значит «здесь владелец видит лицо», даже если сама зона лежит вверх ногами.

Резолвер ЧИСТЫЙ: три входа-данных, ни дерева, ни часов. Отсюда и следует, что поза покоя
авторитетна, а полёт к ней локален. Правила, читающего ОТКУДА груз прилетел, нет и не будет:
траектория есть только у того, кто её видел, и у позднего зрителя ответ разошёлся бы.

| id | Дано | Когда | Тогда |
|---|---|---|---|
| `pose.stock-grains-install-under-their-names` | `installStockGrains()` | спросить реестр | `derive` · `stamp` · `keep` на месте |
| `pose.the-registry-resets-between-suites` | реестр заполнен | `resetGrains()` | пусто: процесс один на все сюиты, протечки быть не должно |
| `pose.no-poser-keeps-what-came` | зона без `Poser`, принесено 15° | `restPose` | 15° — отсутствие способности и есть отказ |
| `pose.poser-defaults-to-keep` | `Poser()` без единой грани | `restPose`, принесено 15° | 15°: объявить атом и не назвать грань — не менять ничего |
| `pose.keep-carries-the-angle-in` | `angle: keep`, принесено 15° | `restPose` | 15° ложится в состояние (сценарий C) |
| `pose.derive-takes-the-arrangement` | `angle: derive`, раскладка сказала 7° | `restPose`, принесено 15° | 7°: диктует раскладка |
| `pose.derive-straightens-when-nothing-was-laid` | `angle: derive`, раскладка молчит | `restPose`, принесено 15° | 0° — 15° теряются: молчание сетки значит «прямо» (сценарий C) |
| `pose.stamp-overrides-whatever-came` | `angle: stamp(0)` | `restPose`, принесено 15°, раскладка 7° | 0°: штамп не спрашивает ни ту, ни другую (сценарии D и G) |
| `pose.stamp-holds-a-number-other-than-zero` | `angle: stamp(90)` | `restPose` | 90°: штамп — число, а не «выпрямить» |
| `pose.keep-with-nothing-carried-is-straight` | `angle: keep`, принесено ничего | `restPose` | 0°: у покоя всегда есть ответ |
| `pose.unknown-rule-is-skipped-not-thrown` | `rule: "wobble"`, незарегистрирован | `restPose`, принесено 15° | 15° и сцена жива: незарегистрированное имя пропускается, ценой в одну грань |
| `pose.a-game-registers-its-own-grain` | игра зарегистрировала свою запись | `restPose` | её ответ и применён — шов сценария F |
| `pose.poser-needs-a-container` | `Poser` на узле без `Container` | `restPose`, принесено 15° | 15°: требование не выполнено — атома НЕТ, правило не применяется |
| `pose.side-stock-records-install-under-their-names` | `installStockGrains()` | спросить реестр сторон | `up` · `down` · `keep` на месте — второй реестр, значения не числа |
| `pose.side-defaults-to-keep` | `Poser()` без граней | `restPose`, принесена рубашка | рубашка: объявить атом — не тронуть сторону |
| `pose.stamp-up-shows-the-face-whatever-came` | `side: up()` | `restPose`, принесена рубашка | лицо. Ручной переворот сбрасывается возвратом в зону — следствие одной строки STAMP (сценарии B и G) |
| `pose.stamp-down-shows-the-back-whatever-came` | `side: down()` | `restPose`, принесено лицо | рубашка — сброс (сценарий B) |
| `pose.keep-out-of-a-turned-zone-shows-the-face` | `side: keep()`, карта вытянута из ЗАКРЫТОЙ колоды | `restPose` | лицо: колода была перевёрнутой ЗОНОЙ, бит карты не трогали (сценарий A) |
| `pose.keep-into-a-turned-zone-hides-the-face` | `side: keep()` у перевёрнутой зоны | `restPose`, принесено лицо | рубашка — по дефолту КОЛОДЫ, а не по памяти о том, где карта была (сценарий E) |
| `pose.stamp-in-a-turned-zone-still-shows-what-it-says` | `side: up()` у перевёрнутой зоны | `restPose` | лицо: штамп называет видимое владельцем, поворот зоны подмешает `setFacing` |
| `pose.unknown-side-rule-is-skipped-not-thrown` | `rule: "wobble"` у перевёрнутой зоны | `restPose`, принесено лицо | рубашка: незарегистрированное имя = зона не навязала ничего, то есть `keep` |
| `pose.a-game-registers-its-own-side-reading-the-chain` | запись игры читает фазу `showdown` на борде выше | `restPose` у руки с этой записью | лицо: читается СОСТОЯНИЕ цепочки, а не траектория, поэтому поздний зритель резолвит так же (сценарий F) |
| `pose.side-and-angle-are-grains-of-ONE-pose` | `angle: stamp(0)`, `side: keep()` | `restPose` | `{angle: 0, side: принесённая}` — одна транзакция, грани друг о друге не знают |
