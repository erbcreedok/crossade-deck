// ХАРАКТЕРЫ БОТОВ — четыре, подтверждённые владельцем. Это данные, а не код: новый характер
// добавляется строкой, и ни один мозг от этого не меняется.
//
// `waitMs` у каждого свой по прямому слову владельца: за столом из четырёх ботов ходы не должны
// падать строем. Две секунды — середина, вокруг которой они разошлись.

import type { Profile } from "./brain.js";

export const PROFILES: Record<string, Profile> = {
  копитель: {
    key: "копитель",
    name: "Копитель",
    says: "Я берегу козыри и джокеров до конца партии. Дешёвую карту лучше взять, чем тратить сильную.",
    hoards: 0.9,
    closes: 0.3,
    presses: 0.2,
    waitMs: 2600,
  },
  закрывала: {
    key: "закрывала",
    name: "Закрывала",
    says: "Я играю на закрытие круга: закрою — открою следующий сам и скину мусор.",
    hoards: 0.5,
    closes: 0.95,
    presses: 0.6,
    waitMs: 1700,
  },
  агрессор: {
    key: "агрессор",
    name: "Агрессор",
    says: "Я бью всегда, когда есть чем, и давлю старшими картами. Брать из круга — последнее дело.",
    hoards: 0.1,
    closes: 0.5,
    presses: 0.95,
    waitMs: 1200,
  },
  новичок: {
    key: "новичок",
    name: "Новичок",
    says: "Я играю как придётся: козыри трачу рано, беру не глядя.",
    hoards: 0.05,
    closes: 0.1,
    presses: 0.4,
    waitMs: 2900,
  },
};

/** Характер по имени; незнакомое имя — «новичок»: бот всегда может сесть за стол. */
export const profileOf = (key: string | undefined): Profile => PROFILES[key ?? ""] ?? PROFILES["новичок"]!;

/** Имена характеров — для команды `bots` и для подсказки в телеграм-боте. */
export const PROFILE_KEYS = Object.keys(PROFILES);
